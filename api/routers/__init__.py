"""FastAPI routers for additive product-domain surfaces (auth-gated writes), kept out of
api/main.py so feature areas evolve independently. Included via app.include_router in main.py.
Each router is off the deterministic decision gate (ADR-0001)."""
