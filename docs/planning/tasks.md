# Task & Progress Tracker

| Field | Value |
|---|---|
| **Status** | Active |
| **Last updated** | 2026-07-10 (MST) |
| **Purpose** | Top-layer session input (read alongside the ToC): tracks development, timeline, and flags parallel-safe work so non-blocking items can fan out to background agents. |

## Timeline

Due **Mon Jul 13, 2026, 6:00 pm MST** (9:00 pm ET). Day buckets, not hour
estimates — adjusted as we go.

| Day (MST) | Bucket | Focus |
|---|---|---|
| Tue Jul 7 | Design / research | Concept lock, docs, ADRs, Phase 0 |
| Wed–Thu Jul 8–9 | Build | Provenance/event seam, real-QC ingest, AI layer, dashboard |
| Fri Jul 10 | Harden | Eval vs truth data, error handling, security, doc refresh |
| Sat Jul 11 | Demo | Demo plan, one-pager/deck, UI states |
| Sun Jul 12 | Flex | Overflow, agent #2, risks, dry run |
| Mon Jul 13 | Buffer / submit | Final checks; submit before 6 pm |

## How to use

- **Statuses:** `todo` · `in-progress` · `blocked` · `done`.
- **Parallel-safe** = no unfinished dependency and an isolated area of the repo;
  safe to run concurrently or hand to a background agent.
- Keep this current in the same change as the work. It is the shared board across
  sessions — read it (with the [table of contents](../TABLE_OF_CONTENTS.md)) at
  session start to see what is claimable.

## Roadmap (phases)

| Phase | Focus | State |
|---|---|---|
| 0 | Foundation & hygiene (uv, mypy, ruff, hooks) | done |
| 1 | Design capture + provenance/event seam | in-progress |
| Port | Productionization: FastAPI read-API + React frontend (ADR-0014) | in-progress |
| 2 | Persistence + real QC data | in-progress |
| 3 | Scoped agents + real confidence (triage agent T-015 done) | in-progress |
| 4 | Cloud & IaC | wishlist |

## Task board

| ID | Task | Phase | Status | Parallel-safe | Depends on |
|---|---|---|---|---|---|
| T-001 | Documentation workflow + ADRs 0001–0007 | 1 | done | — | — |
| T-002 | `data/qc_metrics.md` (grounded, cited, breadth-first runbook) | 1 | done | yes | — |
| T-002b | Concrete test-data QC profile ✅ — real GIAB HG002 panel reads through the **full** gate (`scripts/gate_giab.py`): `samtools fastq \| fastp` → real Q30 88.2% / dup 0.006% / reads-PF 99.3% + `mosdepth` 55.8× coverage, gated on the four metrics a fastq+BAM actually yields (cluster-PF is run-level SAV, not gated) → PROCEED. Registry normalizes each real value from its declared unit exactly as for a mock run | 2 | done | no | T-017 |
| T-003 | `reference/domain-primer.md` + `reference/glossary.md` | 1 | done | yes | — |
| T-004 | `requirements/{functional,nonfunctional,constraints}.md` | 1 | done | yes | — |
| T-005 | `design/architecture.md` ✅; the system-view slices (context, components, data-flow, interfaces, storage, workflows, deployment) are consolidated into it + the ADRs, not split into stubs (see ToC note) | 1 | done | partial | T-002 |
| T-006 | Config layer / profiles — consolidated into ADR-0005 + architecture.md §Swappable seams (no standalone doc) | 1 | done | yes | — |
| T-007 | Repo + data layout — consolidated into the CLAUDE.md code map (no standalone doc) | 1 | done | yes | — |
| T-008 | Data docs ✅ (provenance.md, schemas.md, metric_registry.md, licensing.md) | 1 | done | yes | — |
| T-009 | `quality/{evaluation,risks}.md` ✅ (+ demo/demo_plan.md) | 1 | done | yes | T-002 |
| T-010 | ADRs 0008–0014 (issue-taxonomy, corpora, ticketing/API, tooling, agent-scoping, gate-architecture, productionization) | 1 | done | yes | — |
| T-011 | Demo-ready ✅: production-framed `README.md` + `demo/run-of-show.md` (timed 5:00 script, pre-flight + fallback ladder) + `demo/one-pager.md` (judge handout) + `make emit-ledger` making the reproduce-from-log wow moment a clean two-command flow | 1 | done | no | T-005 |
| T-012 | Phase 0 tooling: uv + `pyproject.toml` single source, mypy/ruff, hooks (batch full-eval → Phase 2/T-009) | 0 | done | no | — |
| T-013 | Synthetic run generator ✅ (`pipeguard.synthetic`) + GIAB HG002 subset fetch script ✅ (`scripts/fetch_giab_hg002.py`, accessions manifest; real fetch needs the genomics toolchain → T-017) | 2 | done | partial | T-002, T-008 |
| T-014 | Event bus + provenance ledger (in-memory + JSONL; DB projection → T-023 ✅) | 1 | done | no | ADR-0002 |
| T-015 | QC-triage agent (advisory, stub-first, corpus + retrieval, /triage API) | 3 | done | no | T-014 |
| T-015b | Outbound notify port ✅ + wired into `run_gate` (`notifier=`; `NOTIFICATION_EMITTED` events) ✅ + live Slack send behind a `PIPEGUARD_SLACK_LIVE` opt-in, **verified end-to-end against a real workspace** + `python -m pipeguard.notify` demo CLI ✅ | 3 | done | no | T-015 |
| T-016 | Data strategy doc + label mock_run_01 origin | 1 | done | yes | — |
| T-017 | Real GIAB HG002 panel data through the QC gate ✅ (`scripts/gate_giab.py`): `mosdepth --by` on the fetched panel BAM → real 55.8× coverage + 99%/97% breadth → gated (PROCEED, clears the 30× gate) reusing `run_gate` + registry rules unchanged. Fetch also validated (truth VCF + `samtools -X` reads slice). **Fastq metrics (Q30/dup/reads-PF) now added via `samtools fastq \| fastp` → T-002b.** Contamination (verifybamid2) remains | 2 | done | partial | T-002 |
| T-018 | Frontend design brief + clickable prototype (`design/frontend/`) | 1 | done | yes | — |
| T-019 | Align confidence to "omit until grounded" (models.py `confidence` → Optional/None, drop demo Confidence tile, update README:32/:105) — part of the models→schemas.md rework | 1 | done | no | T-008 |
| T-020 | FastAPI read-API over the core (`api/`; production seam, ADR-0010/0014) | Port | done | no | — |
| T-021 | React frontend scaffold + design tokens (`frontend/`; Vite + Tailwind, ADR-0014) | Port | done | no | T-020 |
| T-022 | React screens ✅: run overview · decision cards · triage · provenance · monitoring · settings · review queue · intake/preflight (all prototype screens built) | Port | done | partial | T-021 |
| T-022b | Frontend **1:1 fidelity migration** ✅ — rebuilt all 8 screens (§1–§8) on the light content tokens to match the maintainer handoff (`design/frontend/`), verified screenshot-by-screenshot vs the prototype. Run-scoped Intake (§2) + Agent triage (§6, new) routes like Provenance; §4 review-queue tickets w/ reviewer-vs-approver RBAC + `rule_id`-keyed recurring-issue banner; §7 monitoring + §5 provenance compute-DAG. Legacy dark tokens removed. Screens state their data boundary (no fabricated InterOp/compute artifacts) | Port | done | yes | T-022, T-037 |
| T-023 | SQLite persistence: Repository port + event→row projector + rebuild-db (ADR-0002/0003) | 2 | done | no | T-014 |
| T-024 | Metric registry as code (`metrics/`; registry.yaml + typed loader + `MetricValue` + normalization, per metric_registry.md) — additive slice ✅ | 2 | done | partial | T-008 |
| T-025 | Metric registry on the critical path ✅ (step-by-step): QCMetrics→MetricValue mapping (`metrics/mapping.py`) → runbook keys on `our_key` in canonical decimals + units contract in schemas.md → rules gate on `normalized_value` (verdicts byte-identical; display renders back to raw units via `registry.denormalize`) | 2 | done | no | T-024 |
| T-026 | Restructure agents into `agents/<scope>/` — trigger FIRED (agent #2 landed, T-058) but **still deferred** by its own "don't do it mid-dev on other tasks (import-path churn)" clause — the frontend redesign is in flight. #2 landed as a sibling core module (`pipeline_repair/`); the eventual move is `triage/` + `pipeline_repair/` → `agents/{triage,pipeline_repair}/`. **Keep `synthesis/` (narration) and `notify/` (port) OUT** — they aren't agents (ADR-0001). Do it in a quiet window. | 3 | todo | no | T-058 |
| T-027 | Read-API policy + telemetry seams ✅ — `GET /api/runbook` (flattened QC thresholds, disclaimer + `units_note`: canonical-unit gates, illustrative-not-clinical) + `GET /metrics` (hand-rolled Prometheus text exposition; verdict/gate counts; no new dep) + frontend `MetricsPanel` surfacing per-sample `metric_values` on the card + Settings display-unit fix (canonical→display; relative borderline band). Both agent branches adversarially reviewed, fixes folded | Port | done | no | T-020, T-025 |
| T-028 | Design capture ✅ — agent-layer hub [`design/agents.md`](../design/agents.md) (roster + shared invariants + intake checklist) + data-platform/export/run-browser/archivist design [`design/data-platform-and-archivist.md`](../design/data-platform-and-archivist.md) (draft for review; tiered already-built/build-now/target-state; 7 open questions) | 1 | done | yes | — |
| T-029 | NGS analysis-output **layout** convention ✅ — expanded data-platform §3 (3.0–3.6) + Appendix C: a fact-checked tool-output catalog (10 stages, `(VERIFY)`-tagged for ungrounded sarek names) + artifact→disposition map (GATE-READ / FLATTEN-INPUT / INDEX-ONLY) + a coherent grouped tree + a storage policy (panel scale → nothing to delete; BAM→CRAM deferred; keep `fastp.json` as the QC keeper). Additive; the flat `run/` five-CSV ingest stays frozen. Surfaced a naming nuance: `pct_reads_identified` is a fastp pass-filter rate, not a barcode-ID metric (maintainer decision pending) | 2 | done | no | T-017 |
| T-030 | BUILD-NOW data slice — **API export ✅ DONE**: read-only `GET /api/export` over the live-recomputed cards (`grain=decision` CSV + `grain=feature` JSONL = the ML corpus + `format=parquet` via the optional `[parquet]` extra; single file on demand — D3; `X-PipeGuard-Export-Source: live-recompute`), extended by the de-id policy (T-040) and covered by 8 export tests. **Still open (small):** the frontend `RunsBrowser` + download button (no `api.export` client method / export UI yet). `api/`+`frontend/` only; zero core change | Port | in-progress | no | T-028 |
| T-031 | Variant-gate substrate ✅ (**design done**, Appendix D) — layered **CMRG-spine panel** (CMRG ships its own truth VCF+BED; two-truths/two-BEDs routing so each region grades against its own answer key) + **caller-agnostic VCF ingest** + a load-bearing **framing contract** (banned phrasings; ClinVar is fixture-selection/annotation only, **never a runtime gate input**). **Build-now-if-time (zero core change):** `gate_giab.py --call` (bcftools on the on-disk panel BAM) → **EVAL-030** (hap.py/`isec` P/R/F1 vs NIST truth). **Phase 2:** `parse_vcf` + the variant **rules** (the registry already declares inert `variant.*` keys — §8). Guardrail: HG002 is a benchmark genome, not a patient; variants are ClinVar-labeled **test fixtures** | 2 | done | no | T-017 |
| T-032 | Config file for tool **output paths** (artifact-kind → path/glob; typed pydantic-settings + YAML like `metric_registry.yaml`; tolerant; default profile = the frozen five-CSV `run/` contract). Config layer [ADR-0005](../adr/ADR-0005-config-layer-and-profiles.md); the machine-readable seam a canvas (#11) would generate | 2 | todo | partial | T-029 |
| T-033 | Pipeline **run-state capture** → mission-control (per-step execution state, start→end, as ledger events → dashboard projection). Interact with Nextflow, don't reinvent (wishlist #20, Re:1a) | 3 | todo | no | — |
| T-034 | Rename `pct_reads_identified` → its fastp pass-filter semantics (metric registry key + `mapping.py` + runbook + docs). Document-now, rename-eventually (D14) | 2 | todo | yes | T-025 |
| T-035 | Notify webhook adapters: Teams + Discord (stdlib POST, SlackNotifier shape, offline-default, live opt-in) — Jira ticket-create deferred to ticketing phase | 3 | done | yes | T-015b |
| T-036 | Telemetry connector bundle (W17): vendor-neutral pull/scrape configs — Datadog OpenMetrics + Prometheus + OTel Collector (+ optional compose) — over the built /metrics seam, plus docs/ops/telemetry-connectors.md; defer in-app push SDK/OTLP exporter ✅ — configs in `deploy/telemetry/`, ops doc + ToC/wishlist crosslinks; config+docs only, no core/api change, no new dep; no-PHI claim verified against `_render_prometheus` | 3 | done | yes | T-027 |
| T-037 | Provenance canvas ✅ — built as the §5 **compute DAG** (intake→demux→qc→align→variant→gate) with the 3 gate checkpoints, per-stage status from real `gate_results`, and a data-I/O drill-in (name · sha256 · size · origin). New read-API `GET /api/runs/{id}/artifacts` grounds it: real streamed sha256 + on-disk size + run origin tag, **8 MiB hash cap** so raw reads are size-listed not slurped, SampleSheet→demux. Stages we don't execute (align/variant) shown as "not run in this build". **Supersedes** the earlier event-swimlane approach (W10) | Phase 2 (frontend) | done | yes | none |
| T-038 | Metric-catalog read-only view: GET /api/metrics/registry (all 20 registered metric types + gated-vs-registered flag) + Settings "Metric catalog" panel; read-only, zero core change ✅ (merged, 484cc5e) | Port | done | yes | T-024, T-027 |
| T-039 | ArtifactStore port + LocalArtifactStore (zero-dep) + guarded S3ArtifactStore (OFF by default, lazy boto3 in new [s3] extra, PIPEGUARD_S3_LIVE opt-in, degrade-to-local) + run_gate_from_store convenience; realizes ADR-0003 §3 artifact-store wishlist. Defer Box/Drive/OneDrive/DNAnexus/Databricks/Snowflake/BigQuery/Redshift connectors (scope-only) | 3 | done | yes | ADR-0003, T-015b |
| T-040 | Config-driven de-id policy at the export seam ✅ (`api/deid.py`: per-field DROP/HASH/GATE_BY_ORIGIN/PASSTHROUGH + origin-gated, pseudonymized `include=identity` cohort-key opt-in, `X-PipeGuard-Deid-Policy` header; routed through `_decision_rows`/`_feature_rows`; `api/` only, no new dep, stdlib hashlib; demo seam, NOT HIPAA; non-laundering §5e enforced — origin read-only, guarded set fixed) | Port | done | yes | T-030 |
| T-041 | Containerize API + built frontend (deploy/Dockerfile.api multi-stage + docker-compose + .dockerignore; env-configurable DATA_ROOT/CORS + optional StaticFiles mount in api/main.py) — the ADR-0003 containerization prerequisite; ship target-state Terraform (ECR + App Runner) UNAPPLIED, HealthOmics deferred to T-033 | 4 | todo | yes | — (api/ + frontend/ already built) |
| T-042 | In-app feedback capture ✅ (W12) — the app's **first write endpoint**, off the gate. `api/feedback.py` (FeedbackContext/In/Ack, `extra="forbid"` PII guard + disjoint-target model_validator, server-fixed env JSONL, locked json.dumps writer) + `POST /api/feedback` (201; server-authored id/received_at/origin-via-`_run_origin`; OSError→503 no-leak; CORS +POST). Two UI surfaces: per-decision thumbs footer (`DecisionFeedback`, keyed to verdict+gate+rule_ids+content_hash) + a global product FAB (`FeedbackWidget`, mounted in Layout so every screen stays byte-identical). Designed via a 3-angle panel (scoped hybrid) + adversarially reviewed (security/correctness/guardrails clean). 9 new tests; gitignored store; no core change | Port | done | yes | T-020, T-021 |
| T-043 | Postgres port ✅ (ADR-0016) — realize the `Repository` port's "SqliteRepository → Postgres later" seam + move feedback into a DB, all **OFF by default** (offline demo unchanged; mirrors the S3 seam). `PostgresRepository` (ON CONFLICT upserts, JSONB/TIMESTAMPTZ, pipeguard_meta version) + `get_repository()` (PIPEGUARD_REPOSITORY, degrade-to-SQLite, never leaks the DSN) + `rebuild-db` targets either backend. Feedback → pluggable `FeedbackStore` (jsonl/sqlite/postgres, `api/feedback_store.py`) with its own `feedback` table (never the Repository); SqliteFeedbackStore proven E2E in tests. Required `source` trace field (#3a). Advisory **feedback agent** (`api/feedback_agent.py`, stub/claude, PII-safe aggregate-only Claude path, #3b). `[postgres]` extra (psycopg) + mypy override + `deploy/postgres/docker-compose.yml`. FAB made icon-only-on-hover (#1). Adversarially reviewed (4 read-parity fixes: UTC-normalized TIMESTAMPTZ, seq insertion-order). **Validated live against real Postgres 16** (`tests/test_persistence_postgres_live.py`, compose-gated + skip-safe): projection byte-parity vs SQLite, idempotent replay, feedback JSONB round-trip — all pass, no adapter bug. 225 tests + 3 live-skipped | Port | done | yes | T-042, ADR-0003 |
| T-044 | Pipeline Builder MVP ✅ (#11 / W11) — the editable superset of the Provenance canvas (`frontend/src/screens/PipelineBuilder.tsx`): sub-header toolbar (Edit/View · profile switcher · Tidy/Validate/Emit) + three-pane workspace (palette · dot-grid H-scroll canvas · node inspector) + a validate/emit console with a live `run_layout.yaml` preview across default/giab_panel/sarek. Renders the 7 seeded germline tool nodes (pg-status badges, typed ports, reference hollow rings) + edges + reference nodes + the deterministic-ingest band + the terminal locked gate + the port-less advisory QC-triage agent. Composes-never-executes; hard invariants shown as visible guarantees. Built from the refreshed handoff; reuses the shell + tokens. Nav item under Configure | Port | done | yes | T-018, T-022b |
| T-045 | Frontend fidelity + scale backlog — from a 6-agent fidelity/scale audit (2026-07-09) synthesized into a claude-design handoff ([`design/frontend/handoffs/2026-07-09-review-to-design.md`](../design/frontend/handoffs/2026-07-09-review-to-design.md)). **fix-now bucket ✅ DONE** (02db752, fanned out 6 write-agents one-per-screen, disjoint files, tsc/lint/build + screenshot-verified): Decision-cards density lever (Dense = compact row, was empty) + verdict left-stripe + expand-all + empty-filter + all-evidence; Runs skeletons/Retry/tooltips/clear-filter; Agent-triage verdict-tinted chips + scrollable thread + quick-asks; Monitoring drill-throughs + top-N + honest window label + repair-agent button; Settings working Lean/Granular + editable thresholds + cost column; Pipeline-Builder port-anchored edges + click-to-focus validation + palette search + controlled params + wired Copy/Download/Emit. **Phase-2 backlog ✅ CLOSED via T-062** (frontend design-replication, 2026-07-09): shared scale kit (search/facet/paginate, client-side — see [data-platform-and-archivist.md §4.5](../design/data-platform-and-archivist.md)) ✅, synthetic volume (T-050) ✅, windowed backend aggregates (T-048) ✅, `RunSummary` status/date/platform (T-047) ✅, Decision-card QC-readout (T-055, now wired into `RunDetail`) ✅, Pipeline-Builder editable locators→live YAML (Wave 3) ✅, save/version/approval UI (Wave 3, **optimistic-local** — see T-062) ✅, per-agent model tiering (Settings screen, **UI-only, not wired to `PIPEGUARD_*_MODEL`**) ✅. List **virtualization** stays deliberately out of scope (§4.3 honest scale framing — not needed at demo volume). **Design-question items resolved:** scale interaction per surface → "everything, faithfully" (maintainer decision, T-062 journal); threshold-edit RBAC/audit → T-051/T-052/T-053; node-authoring agent → stays T-046 (proposed, not built; its Builder modal is a static `phase-2`-labelled preview only) | Port | done | yes | T-022b, T-044, T-062 |
| T-046 | Node-authoring agent (roster #5, **proposed — design note**) — graduated the #11 "bring your own tools" concept from a brief line into a tracked design item: [`design/node-authoring-agent.md`](../design/node-authoring-agent.md) + roster row #5 in [`design/agents.md`](../design/agents.md). Advisory, **authors a `ToolNode` card never a run** (draws no edge, never on the gate, never a verdict — ADR-0001); drop a tool's docs (`nextflow_schema.json` / `--help` / module / README) → propose a typed card (ports by `ArtifactKind`, param form, candidate locators) → human reviews/edits/accepts → joins the palette. **Layered stub-first**: the deterministic nf-core schema → param-form importer **is wishlist #9** ($0 core); Claude only for the fuzzy `ArtifactKind` mapping + unstructured docs (opt-in, degrade-to-stub). Build order = review-card UX + palette-injection seam → importer (#9) → LLM layer. Not built; covered by the existing agent ADRs (0001/0006/0009/0012) when it graduates | Port | proposed | yes | T-044, W9 |
| T-047 | Run lifecycle status + platform + date ✅ (A) — the backend half of the T-045 "RunSummary status/date/platform" item, fixing a real bug: the runs list inferred "Released" from `n_attention===0`, so a still-running run with 0 attention was mislabeled Released. New `parse_sample_sheet_header()` reads the Illumina v2 `[Header]` (InstrumentPlatform/Date/RunName, missing→None, no datetime fabricated); `RunArtifacts` gains `platform`/`run_date`/`run_name`; API `RunSummary` gains `platform`/`run_date`/`status` (honest tri-state from provenance: `running` until `ANALYSIS_RUN_COMPLETED`, then `needs_review` if actionable else `released`). A run-lifecycle label, NOT a verdict. Additive/backward-compatible; frontend wires it later (contract handoff). Commit 7ebf660 | Port | done | yes | T-045 |
| T-048 | Windowed monitoring endpoint + runs pagination/search ✅ (C) — the backend half of the T-045 "windowed backend aggregates" + scale-kit items. `GET /api/runs` gains `verdict/q/sort/page/limit` (no params → byte-identical; total/page/limit headers, CORS-exposed). New `GET /api/monitoring?window={7d|14d|30d|all}` returns the pre-aggregated dashboard (KPIs incl. an `auto_proceed_pct` **heuristic**, per-run rows, per-gate flagged/total, ranked recurring signatures), replacing Monitoring.tsx's client-side N-fan-out; aggregation reuses `_aggregate_metrics()` in the API layer. Windowed views drop dateless runs (counted). Commit 7ebf660 | Port | done | yes | T-047 |
| T-049 | Pipeline Builder save/version store ✅ (D) — the backend half of the T-045 "Pipeline-Builder save/version/approval" item. `api/pipeline.py` + `api/pipeline_store.py`: a PRODUCT-domain store OFF the gate (distinct from the decision `Repository`), pluggable JSONL/SQLite/Postgres via `PIPEGUARD_PIPELINE_STORE` (degrade-to-JSONL, never logs DSN), mirroring FeedbackStore (ADR-0016). `POST/GET /api/pipelines` (+ `/{name}` versions); server-authored monotonic per-name version. `PipelineGraph` = a **tolerant versioned envelope** (arbitrary `graph` JSON kept as-is). **Reconciled to the maintainer's builder-versioning decision**: reserves a draft→save→approve lifecycle (`status` draft/pending_review/approved) + reviewer/approver RBAC (`*_by`, server-authored, no PII via `extra="forbid"`); approve-transition + auth are a not-yet-built seam. Commit 7ebf660 | Port | done | yes | T-044 |
| T-050 | Synthetic data at scale ✅ (B) — the backend half of the T-045 "synthetic volume" item. `src/pipeguard/synthetic/scale.py` drives the existing generator at volume (one large run + many-run batches) with zero-padded IDs (fix the log-rule `sid in line` substring cross-fire) + a realistic failure spread. CLI gains `demo/scale/bulk`. Committed one 30-sample run (`data/mock_run_scale_30/`, all 4 verdicts); bulk regenerates on-demand into git-ignored `data/synthetic_bulk/`. Fixed the origin-label drift (`generate_run` now writes the marker; `contrived` end-to-end; mock_run_02/03 corrected). Commit 44c898a | Port | done | yes | T-045 |
| T-051 | Config/settings authoring store ✅ (was deferred; built once design's `settings_editing` decision landed). `api/routers/settings.py` + `api/settings_store.py`: a config-override authoring store with a `draft→save→approve` RBAC lifecycle + audit + lenient sanity guardrails; a tolerant versioned envelope; pluggable JSONL/SQLite/Postgres (`PIPEGUARD_SETTINGS_STORE`, degrade-to-JSONL). `POST /api/settings/thresholds` (draft, reviewer+), `GET` (latest per name), `GET /{name}` (versions), `POST /{name}/approve` (approver-only). Does NOT mutate the live runbook — applying an approved override is a documented off-gate seam. Consumes T-052 auth. Commit e096bd7 | Port | done | yes | T-049, T-052 |
| T-052 | Identity + RBAC primitive ✅ (ADR-0017) — the cross-cutting unlock the design's draft→approve flows need (UI hardcoded `a.rivera/Reviewer`). `api/auth.py`: Role viewer\|reviewer\|approver, `Actor{id,role}`, `current_actor()` reading `X-PipeGuard-Actor/-Role` headers with a permissive DEV-DEFAULT (id=dev, role=approver so offline demo/tests need no wiring), `require_role(*roles)`→403+Actor. A documented DEV SHIM (header-trust); `current_actor` is the single swap point for a real provider. `POST /api/pipelines` now auth-gated + captures `submitted_by`. Commit e096bd7/7855333 | Port | done | yes | ADR-0017 |
| T-053 | Review-queue / ticket domain ✅ — the writable queue the Monitoring escalation + triage screens assumed but didn't exist. `api/routers/review_queue.py` + `api/review_store.py` (pluggable, `PIPEGUARD_REVIEW_STORE`): `POST /api/review/tickets` (reviewer+), `GET` (filter status/run_id/rule_id), `POST /{id}/action` (acknowledge/resolve/escalate/suppress/reopen; resolve/suppress approver-only). Ticket `open\|in_review\|resolved`, action audit trail, OFF the gate (never changes a verdict). Records review-action timestamps for a later median-review-time KPI. Consumes T-052. Commit e096bd7 | Port | done | yes | T-052 |
| T-054 | Pipeline approve/dry-run/diff lifecycle ✅ — realizes the draft→approve lifecycle T-049 only reserved. `api/routers/pipelines_lifecycle.py` (extends `api/pipeline_store.py`): `POST /{name}/submit` (draft→pending_review, reviewer+), `/approve` (approver-only, sets `approved_by` + the diff baseline), `/dry-run?run_id` (READ-ONLY locator resolver vs a real run dir — **compose ≠ execute**, never triggers a run), `GET /{name}/diff` (vs last emitted). Consumes T-052. Commit e096bd7 | Port | done | yes | T-049, T-052 |
| T-055 | Decision-card QC-readout projection ✅ — the "hero" Metric·Observed·Threshold·Status table as an **API-layer projection** (`api/card_readout.py`), joining the card's `metric_values` with runbook `QCThreshold`, so the core `DecisionCard`/gate is untouched. `GET /api/runs/{id}/cards/{sid}/qc-readout`. (Core-side card enrichment — per-gate sub-verdict rollup, linked-sample, provenance metadata, context-rail fields — still owed for the full design, deliberately off this burst.) Commit e096bd7 | Port | done | yes | — |
| T-056 | Runs-list Tier-0 reconciliations ✅ — bind the design to the shipped API. `GET /api/runs` gains a `status` filter (running\|needs_review\|released), `q` now matches run_id OR platform (case-insensitive), friendly sort aliases recent/urgent/date, and per-status facet counts on an `X-PipeGuard-Status-Counts` header (CORS-exposed). Kept `needs_review` (not renamed to the design's `review`; frontend maps for display). Commit 7855333 | Port | done | yes | T-047, T-048 |
| T-057 | Tier-2 north-star backend (from the evolved prototype) — **partially landed.** Of the four bundled items (run submissions/ingest, a BaseSpace connector, conversational multi-turn triage chat, a run hand-off), only **ingest/hand-off shipped** (commit `e77c2e6`, 2026-07-09): new `api/routers/intake.py` — `POST /api/runs` registers a run + triggers `scripts/run_giab_pipeline.py` as a background subprocess (in-process job registry; `require_role(reviewer\|approver)`; 409 on a dup run id; only `HG002`-fixture samples are processed, the rest honestly skipped, 422 if none) and `GET /api/runs/{id}/intake-status` polls `queued\|running\|complete\|failed`. `Submit.tsx` now submits → polls → navigates to the new run's cards. This is genuinely an **execution boundary**, not just registration — `src/pipeguard/` still never runs a tool (compose ≠ execute holds at the core, ADR-0001/0003), but the API layer now DOES trigger an external driver as a background job, closing the "Submit never runs anything" gap flagged below. **Still not built:** a BaseSpace connector (list/import + OAuth) and conversational multi-turn triage chat — still wishlist. (The pipeline-repair #2 + archivist #3 agents it originally bundled are T-058/T-059, done.) | Port | in-progress | yes | T-052, T-054, T-062 |
| T-058 | Pipeline-repair agent ✅ (roster #2, ADR-0008/0012) — advisory, **off the gate**. New core module `src/pipeguard/pipeline_repair/` mirroring `triage/`: given a `RecurringSignature` from the monitoring rollup (`assemble_recurring_signatures` mirrors `get_monitoring`'s `Finding.signature` counter), retrieves a curated remediation corpus (`knowledge/pipeline_repair.jsonl`, 11 entries grounded in ADR-0008's taxonomy + the runbook, no invented thresholds) → a cited `RepairProposal{summary, attach_to(stage), scope(gate)}` with `advisory=True`, NO verdict. `PIPEGUARD_PIPELINE_REPAIR_AGENT=stub\|claude` + `_MODEL` (Opus-high). On-demand `GET /api/monitoring/signatures/{signature}/repair`; the ~3× auto-escalation stays deferred. 16 tests. Commit e3889b3 | 3 | done | yes | T-015, T-048 |
| T-059 | Archivist agent ✅ (roster #3, data-platform §5) — advisory "librarian", **off the gate**. New `api/archivist.py` mirroring the feedback agent: indexes/summarizes already-decided runs from the projection → `ArchiveDigest` (digest + export manifest + cross-run index). Organizational not diagnostic; least-privilege input (no subject_id/tissue/submitted_by); never opens/moves/deletes a file or relabels an origin. `PIPEGUARD_ARCHIVIST_AGENT=stub\|claude` + `_MODEL` (Haiku). `GET /api/runs/{id}/archive-digest` + `GET /api/archive/index`. 17 tests. Commit e3889b3 | 3 | done | yes | T-042, T-039 |
| T-060 | Doc-keeper subagent ✅ — codify the repo's documentation contract as a reusable Claude Code custom subagent [`.claude/agents/doc-keeper.md`](../../.claude/agents/doc-keeper.md) (dev tooling, **not** a product/roster agent). Four modes (SWEEP / AUDIT / AUTHOR / CHK); it **points at the living sources** (Doc-update map, `_templates/`, DOCUMENTATION_HABITS) rather than a hardcoded snapshot, and carries the durable invariants (map-is-routing-authority, ground-claims-in-code, journal-is-archive, ISO-MST + crosslinks, life-science honesty). Noted in [DOCUMENTATION_HABITS.md](../DOCUMENTATION_HABITS.md); loads at next session start. | 1 | done | yes | — |
| T-061 | Execution-trace ingestion (EXEC-001) ✅ — pull structured issues from a pipeline EXECUTOR's trace for the repair agent (the (b) piece; closes the "blocked on execution_trace capture" gap). `parsers.parse_execution_trace(trace.txt)` (tolerant, sniffs tab/comma) → `models.TraceRecord{task_id,process,tag,status,exit}` on `RunArtifacts.execution_trace` via `load_run`; `rules._check_execution_trace` = **EXEC-001** (nf-core `tag`==sid EXACT so no substring cross-fire; status in the runbook's `trace_failure_statuses` or a nonzero `exit` → a cited PIPELINE/preflight Finding → RERUN). PIPE-001's structured sibling; **on-gate** (maintainer call), preserving compose≠execute (READS a trace, never runs — ADR-0001/0003). Flows to the pipeline-repair agent (#2) via the recurring-signature rollup + a targeted EXEC-001 corpus entry. Synthetic `PROCESS_FAILURE` mode + conditional `trace.txt` (excluded from the scale auto-spread → committed runs byte-identical). 8 new tests (`test_execution_trace.py`) + a synthetic `PROCESS_FAILURE` case; pinned verdicts unchanged. Commit e79a319 | 2 | done | yes | T-058 |
| T-062 | Frontend design-replication ✅ — faithful rebuild of the React app (`frontend/src/`) to the refreshed design prototype (`docs/design/frontend/`, dc.html 3360 lines) that landed 2026-07-09. Preceded by a 14-way read-only gap analysis ([journal 2026-07-09](../journal/2026-07-09-frontend-design-replication-scope.md): 28 flags + 5 invariant guardrails) and a maintainer scope call ("everything, faithfully" incl. the Pipeline Builder XXL track + serif adoption). **Wave 1 (foundation, commit 7f55f60):** design tokens (`--font-serif` Newsreader, restored `--color-page`, `--color-nav-active`), a much larger `types.ts` data contract (RunStatus, RunsPage, Role/Actor, Monitoring\*/CardReadout/RunbookPolicy/Pipeline\*/Ticket\*/ThresholdOverride\*/AgentProposal families), a rewritten header-aware+RBAC `api.ts`, shared `context/RoleContext.tsx`, a 2-group nav (Operate/Configure) + interactive user panel, shared primitives (`PageHeader`/`SegmentedControl`/`FacetChip`/`CollapsibleRow`/`useRefresh`), and a new Submit-samplesheet screen/route. **Wave 2 (commit 40f829b):** all 9 operator screens rebuilt (Submit, Runs scale-kit, Intake, Decision-cards QC-readout hero, Review queue RBAC, Provenance origin-chips removed, Agent triage multi-line composer, Monitoring on one `/api/monitoring` call, Settings assay+model-tiering+notifications); dead `TriagePanel.tsx` removed. **Wave 3 (commit 89f353c):** Pipeline Builder rebuilt (`PipelineBuilder.tsx` + 6 `Builder*` components) — free composition, typed-port Connect mode, minimap, Validate/Diff/Dry-run console, 4 hand-off/authoring modals. **Repo hygiene** (commit ccbc548): removed a stale root duplicate of the design package + root-anchored `.gitignore` guards. Backend **not** touched. **Known, labelled deferrals** (no fabrication): Monitoring `first_seen`/`last_seen`/`trend` + Median-review (no backend field); Provenance artifact URLs (`RunArtifact` has no `url`); Builder Save/Approve/Diff/Dry-run render optimistic-local (fire-and-forget) rather than reading the server response; Settings per-agent model tiering is UI-only (not wired to `PIPEGUARD_*_MODEL`); Submit has no backend endpoint (T-057). Closes the T-045 phase-2 backlog. Supersedes/extends T-022/T-022b (1:1 fidelity migration). Docs swept: [architecture.md](../design/architecture.md), [data-platform-and-archivist.md §4.5](../design/data-platform-and-archivist.md), [functional.md](../requirements/functional.md) REQ-F-042/045, [scope-and-wishlist.md](../requirements/scope-and-wishlist.md) #11, `CLAUDE.md` code map | Port | done | yes | T-022b, T-044, T-045 |
| T-063 | Real GIAB HG002 fastqs through the outlined pipeline, end to end ✅ — `scripts/run_giab_pipeline.py`: unlike `gate_giab.py` (reads the pre-aligned NIST panel BAM), this **executes** the Pipeline-Builder germline chain from raw reads (`fastp → bwa-mem2 mem → samtools fixmate/markdup → {mosdepth, bcftools call → norm}`) on the real GIAB HG002 panel fastqs, writes a dashboard-discoverable run dir (`data/RUN-2026-07-08-GIAB-HG002/`, frozen-five CSVs + narrated `pipeline.log` + `origin=real-giab`), and runs the **unchanged** `run_gate`. Verified live: `GET /api/runs` discovers it (`needs_review`, HiSeq 2500, 2026-07-08); the decision card renders real Q30 88.2% / reads-PF 99.3% / 54.2× / dup 0.006% / 553 variants and gates to HOLD on the honest cluster-PF-missing signal (a run-level SAV metric a fastq→BAM path can't produce — flagged, not fabricated). Compose ≠ execute intact (a standalone bioconda-toolchain driver; the app only ingests `run/` outputs); reference + intermediates + the run dir are git-ignored, nothing raw committed. `docs/journal/2026-07-09-giab-e2e-pipeline.md`. Commit 64a6173 | 2 | done | yes | T-017 |
| T-064 | Frontend batch-2 Wave 0: populate the app + fix specific UI complaints ✅ — `scripts/seed_giab_demo.py` seeds ~24 synthetic (`origin=contrived`) GIAB-named runs into `data/` (git-ignored; script is the reproducible record; `--tickets` also POSTs a few Review-queue tickets) so every screen has volume (29 runs total, 8 released / 21 needs_review, 4 platforms) — pagination/facets/Monitoring windows all engage. Also fixed "decision page missing QC checks": the QC-readout hero was hidden whenever a card's `metric_values` was empty; the seeded runs carry real FASTQ-derived metrics so it populates. New `RunSpec.platform` field on the synthetic generator (default `NovaSeq`, backward-compatible) makes the platform facet testable. Restored the Analyze nav divider (Operate/Analyze/Configure, reversing T-062's 2-group call per maintainer feedback); `FacetChip` count renders as a filled pill (was bare mono text, shared fix across Runs/Decision/Review-queue); `RoleContext.setActor` gained full actor (id+role) switching (unblocks Admin's "Act as", T-066); removed dead Vite-scaffold assets. Commit e891e62 | Port | done | yes | T-062 |
| T-065 | "New pipeline" affordance for the Builder ✅ — the maintainer couldn't create a new pipeline (the Builder only ever opened the one seeded germline graph; the profile combobox is a locator-config switch, not a document-level "new"). Toolbar "+ New" button opens a modal (Blank graph or From-template-germline, optional name) that opens a fresh Draft in Edit; View-default still guards the existing linked pipeline from accidental edits. `BuilderCanvas` gained a `showSeeded` prop so a blank doc renders empty (terminal Decision gate + ingest band + user nodes still always render); only the original seeded doc reads "Linked · RUN", a new draft shows "Draft — not run". Compose ≠ execute preserved. Commit 327c2d2 | Port | done | yes | T-044, T-062 |
| T-066 | Admin panel ✅ — new `/admin` screen, approver-gated (the de-facto admin role), off the deterministic gate: it governs WHO may perform off-gate writes and whose id lands in audit fields, never a verdict/finding/confidence (ADR-0001, README §8; no confidence meter). Three tabs in [`frontend/src/screens/Admin.tsx`](../../frontend/src/screens/Admin.tsx): **Users & roles** — explicit **client-mock** (`api/auth.py` is a header dev-shim with no backend user store) — a seeded roster with a 3-way `RoleSelect` + "Act as" wired to `RoleContext.setActor`, plus a persistent "dev auth shim, not an identity system" banner; **Activity log** — 100% real reads, merging `listThresholds`+`listPipelines`+`listTickets` into one append-only audit feed, `FacetChip`-filterable by kind; **System** — 100% real reads (new `GET /api/health` + `api.health()`, runbook gate count, metric-registry version/gated) with the illustrative-not-clinical disclaimer. Nav gains an approver-only "Admin" group; `UserPanel` footer now reflects the live actor. Verified live (3 tabs render, Act-as switches actor+role+nav, 15 real activity rows). Commit ce396f7 | Port | done | yes | T-052, T-062 |
| T-067 | Frontend↔backend write wiring: fix 3 broken product-write paths + surface real outcomes ✅ — every off-gate write fired optimistic-local and swallowed its response; three paths 4xx'd against the real API. **(a)** Threshold save/approve (`SettingsAssayTable`): the override name was `thresholds:<display>` — the colon+spaces fail the backend slug regex (422 on save, 404 on approve); now slugified (`thresholds-rare-disease-germline-v3`), reconciled from the ack. **(b)** Pipeline save→submit→approve (`PipelineBuilder`): approve requires `pending_review`, but Save only created `draft` and submit was never called (409); Save now chains `savePipeline` → `submitPipeline`, reconciling version/status from each response. **(c)** Ticket resolve/suppress RBAC (`api/routers/review_queue.py`): the backend required approver for resolve+suppress, but the design (README §5.5) has a **reviewer** resolve hold/rerun tickets; relaxed to `("reviewer", "approver")` (escalate→approver nuance stays UI-enforced); `tests/test_review_queue.py` updated to match. **(d)** New lightweight `Toast` system (`frontend/src/components/Toast.tsx`, `ToastProvider` in `App.tsx`) surfaces the real 403/409/422 instead of silently diverging. Verified: save/submit/approve all 2xx; 13 review-queue tests pass; tsc/lint + ruff/mypy clean. Commit 586f832 | Port | done | yes | T-049, T-051, T-053, T-062 |
| T-068 | Fidelity closers batch 2 (details missed in prototype→live) ✅ — fanned out one agent per screen (disjoint files). **Decision (S3):** a QC-gated card with empty `metric_values` no longer hides the whole QC-readout hero — renders runbook thresholds as "not measured" placeholder rows (`Observed —`, neutral chip) instead of dropping the block; status stays rules-derived. **Monitoring (S4, backend-additive + frontend):** `MonitoringSignature` gains `first_seen`/`last_seen` (ISO date range), `trend` (`up`\|`down`\|`flat` over window halves), `affected_run_ids` (chronological, deduped) — the signature grid renders the date range + a trend arrow + deep-links (`/runs/:id?filter=attention`); `tests/test_api.py`'s exact-key assertion covers all four. **Review queue (S6):** the resolved banner + triage action buttons were `CollapsibleRow` children (hid when collapsed) — now an always-visible card-level footer. **Intake (S5):** surfaces the real yield cutoff ("Yield target ≥ 70% reads identified"). **Settings dialog (S7):** gradient avatar + Change-avatar, default desktop notifications on, 2-column profile grid. **Provenance (S8):** per-stage note fallback; align/variant stay "not run in this build". **Agent triage (S8):** two-line citation labels derived from `source_kind`. tsc/oxlint/ruff/mypy(68) clean; 41 API tests pass; browser-verified. Commit 6371128 | Port | done | yes | T-048, T-055, T-062 |
| T-069 | Wire the Builder's Dry-run/Diff/Export/Archivist actions to the real endpoints (+ saved-profiles) ✅ **CLOSED 2026-07-10 (commit `adfd7aa`).** Dry-run/Diff were already wired by T-096. This commit closes the remainder: `PipelineRepairModal` → `GET /api/monitoring` (a recurring-signature picker) + `GET /api/monitoring/signatures/{sig}/repair` → the REAL `RepairProposal` (summary/rationale/attachTo/scope/`signature_count`, cited corpus refs with a **"heuristic" score label, never "confidence"**; "Send to review queue" navigates to `/queue` — no fabricated ticket, a signature-level fix has no sample_id/verdict; on failure shows the shared honest `FALLBACK_SUMMARY`, empty → "nothing to propose"). `ArchivistModal` → `GET /api/archive/index` → the REAL cross-run `ArchiveDigest` (n_runs/archive-ready, held-from-archival, origins verbatim, proposed action, recurring signatures, size, the backend disclaimer; "Queue archive" stays inert — no write endpoint; honest "unavailable" on failure). `RunHandoffModal` now shows the REAL composed `run_layout.yaml` (`yamlFor`, labelled by profile); the button copies it (+ fires the compose-only Emit) instead of a fake "Hand off to Nextflow" — compose ≠ execute holds, no network call. **Saved-profiles** ships too: a new toolbar "Open" action (`PipelineBuilder.tsx` `LoadSavedModal` + `loadSavedPipeline`) lists `GET /api/pipelines` and hydrates the canvas (nodes/edges/locators/profile/version/status) from a chosen saved graph; approved graphs open read-only, re-saving mints a new draft; honest loading/empty/error, a foreign envelope with no restorable topology loads empty with a labelled toast (never fabricated nodes). New types in `types.ts` (`AgentProposal` enrichment, `RepairCitation`, the `ArchiveDigest` family) mirror the pydantic wire shapes; `api.ts` retypes `archiveDigest`/`archiveIndex` off `Record<string, unknown>`. Every advisory/off-gate/ADR-0001 disclaimer preserved; no verdict/confidence rendered. Verified live: repair shows PROV-001 · demux · preflight · 5 heuristic cites, picker refetches; archivist shows 8/29 archive-ready + contrived=28/real-giab=1; run-handoff shows the real YAML; Open loads a saved v2 (status "pending approval") and hydrates the nodes; tsc + oxlint clean; adversarial review pass. Frontend-only, verified (`git diff --stat a728cb7..adfd7aa -- src/ api/ tests/` empty). [journal 2026-07-10](../journal/2026-07-10-builder-modals-and-run-selector.md) | Port | done | yes | T-054, T-059, T-065 |
| T-070 | Run-selector scalability — a reusable searchable/paginated run picker for surfaces that need to name a specific `run_id` ✅ **CLOSED 2026-07-10 (commit `3c6455e`).** New `frontend/src/components/RunSelector.tsx`: a searchable (by id/platform), 8-row-capped combobox sharing the top-bar switcher idiom — a real status dot via `RUN_STATUS_META` (F17, never `n_attention`) + id + platform + label; dependency-free, self-fetches lazily via `api.runs()` on first open (props allow an injected list too) so no runs list has to thread through a consumer's props. Never fabricates a run: an honest "Couldn't load runs" with no rows on fetch failure (the self-fetch effect guards on `error` in both the condition and deps so a failure settles into a stable state instead of an infinite refetch loop — caught by an adversarial review pass; `close()` clears the error so reopening retries). First consumer: `BuilderConsole.tsx`'s Dry-run tab (`runId` now nullable; the resolve button disables until a run is picked) — replacing the plain text input T-096 shipped. Props (`value`/`onChange`/`runs?`/`status?`/`maxRows?`/`onViewAll?`) make it ready for the archivist/run-handoff surfaces as future consumers (not yet wired there). Verified live: the picker lists real runs, filters by "giab-b", caps at 8, picking updates the trigger + closes the popover; tsc + oxlint clean. [journal 2026-07-10](../journal/2026-07-10-builder-modals-and-run-selector.md) | Port | done | yes | T-048, T-069 |
| T-093 | Admin: paginate the activity log + expandable rows ✅ (A2) — the audit feed rendered every row flat + truncated, a formatting mess as it grows. `Admin.tsx`'s `ActivityTab` gains 25/50/100 pagination (`SegmentedControl` + ‹/›  pager, "Showing X–Y of Z," resets on filter change) and each row is now a compact summary that expands on click to a labelled Detail/Target/Actor/When panel (one open at a time). No backend change — same merged threshold/pipeline/ticket feed (REQ-F-066). Verified live (as admin): 17 rows, per-page control + pager, a row expands to its full detail; tsc + lint clean. Commit `8a14661` | Port | done | partial (same file as T-094) | T-066 |
| T-094 | Admin: build out System (observability + artifact store) + password reset ✅ (A3/A4) — System tab gained an Artifact-store stat card (`local` · the `PIPEGUARD_ARTIFACT_STORE` s3 seam) and an Observability section surfacing the telemetry-stack views — Prometheus `/metrics` (the read-API exporter), Prometheus (`:9090`), and the Grafana "PipeGuard — QC decision gate" dashboard (`:3000`, T-036/T-079) — as links (Grafana blocks framing; the stack is off the offline demo path), with a note on bringing up the compose stack. Users tab gained a per-user password/email-reset action — a production seam (no live mail) that toasts what would happen (a signed, expiring reset link emailed to the user). Verified live (as admin): System shows 4 stat cards + 3 observability links + telemetry note; Users tab has a Reset-password action per user; tsc + lint clean. Commit `7c56564` | Port | done | partial (same file as T-093) | T-036, T-066, T-079 |
| T-095 | Settings: sample-type dropdown for runbook thresholds ✅ (S1) — the threshold matrix showed Whole-blood and Saliva as two side-by-side columns. A Sample-type dropdown (beside the Assay selector) now picks one tissue at a time and the table shows a single value column for it — cleaner and scales as more sample types are added. Editing/save/approve, the per-tissue values, and the audit lifecycle are unchanged (still keyed on assay × sample type, REQ-F-062). Verified live: the Sample-type dropdown (Whole blood / Saliva) switches the single value column; tsc + lint clean. Commit `869cf55` | Port | done | yes | T-045, T-062 |
| T-096 | Pipeline builder: wire Dry-run/Diff to the real endpoints ✅ ("Item E") — closes the long-standing "`dryRunPipeline`/`pipelineDiff` exist but aren't called yet" limitation (narrows T-069). Once the graph is Saved (it exists in the pipeline store), `BuilderConsole`'s Dry-run tab calls `POST /api/pipelines/{name}/dry-run?run_id=…` — a plain run-id text input, **not** yet T-070's searchable run-selector — rendering the real per-locator matched/ambiguous/missing/invalid resolution + summary; Diff calls `GET /api/pipelines/{name}/diff`, rendering added/changed/removed vs the approved baseline (or "no baseline yet"). Both fall back to the client-side preview before Save. Save stamps `savedName` (+ clears prior results); New/Cancel reset it. Compose ≠ execute holds — a dry-run globs paths, runs nothing (ADR-0001/0003). Verified live: New → template → Save → Dry-run returned "v2 vs run mock_run_01 · missing 10" (honest — a frozen run dir has no raw pipeline paths); Diff returned "10 added" (never approved → no baseline); no console errors; tsc + lint clean. Commit `4208f0b` | Port | done | yes | T-054, T-069 |
| T-097 | Review queue: batch-clear holds/escalations (DC2 part 2) ✅ — completes the maintainer's two-tier DC2 model (part 1 = gate-dependency readout, T-087). The DC2 model treats a HOLD as a *state* and an ESCALATE as *requesting input*, both **clearable by the operator individually or in batches**. `ReviewQueue.tsx` now gives reviewers+ a select checkbox on every still-open (open/in-review) ticket plus a per-run select-all in the group subheader; selecting ≥1 raises a sticky batch bar ("N selected — Resolve selected / Suppress selected / Cancel") that reuses the existing backend-persisted per-ticket `act()`/`toggleSuppress()` path (materialize ticket → `ticketAction`), so a bulk clear persists exactly like a single click. Selection resolves against the live ticket list each render (a key that has left the clearable set can't re-fire); viewers see no checkboxes; the bar is gated on `isReviewer` to match the reviewer+approver RBAC on resolve/suppress (REQ-F-063). Off-gate — clearing never moves a verdict (ADR-0001). Verified live as approver: selected 2 tickets → Resolve selected → Open 88→86, Resolved 1→3, and the fully-cleared run left the Open view; **persisted across a full reload**; tsc + lint clean. Commit `a8fd059` | Port | done | yes | T-087, T-063 |
| T-098 | Theme: soften light mode to a warm japandi palette + theme-aware canvas dots ✅ — maintainer feedback that light mode was too glaring/cool-white. `frontend/src/index.css` warms the light `@theme` neutrals toward sand (`--color-page` `#f5f7f9`→`#f2efe7`; `--color-card` `#fff`→`#faf9f4`; `--color-card-2/-3`, `--color-line(-strong)`, `--color-text(-2/-3)` all shifted warm greige; `--color-accent-weak` retuned to match), contrast kept AA+; functional verdict colors, the dark nav, and the blue accent are unchanged. A new theme-aware `--canvas-dot` var (warm+subtle in light `#d6cfbe`, `rgba(150,165,185,.08)` in dark, was a hardcoded light hex that read as distracting on the dark canvas) moves the Pipeline-Builder dot grid onto `BuilderCanvas.tsx`'s scroll surface so it spans the whole canvas, not just the content plane. Purely visual re-skin — no component, gate, or data-contract change. Verified live in both themes; tsc + oxlint clean. Commit `5763be1` | Port | done | yes | T-062 |
| T-099 | UI feedback pass: agent tiles in View, provenance relabel, runs bars, triage pagination ✅ — four disjoint maintainer asks. **(1)** Pipeline-Builder advisory-agent palette tiles (QC-triage/Pipeline-repair/Archivist) gain a new `alwaysEnabled` `PaletteItem` flag and are now clickable in **View** mode (`PipelineBuilder.tsx`) — they only ever open a read-only advisory modal/pill, never mutate the graph, so an operator can consult an agent without switching the whole canvas into Edit; node-adding tiles still require Edit. **(2)** Provenance (`Provenance.tsx`): the artifact digest label changes "hash" → "**fingerprint**" (a content digest is not a process/run id — those are `arun_…`/`evt_…` — so "fingerprint" reads accurately without naming the algorithm, continuing T-080's defense-in-depth framing) and the label now shows the full 64-char value on hover, not just via the existing "show full" toggle. **(3)** Runs list (`RunOverview.tsx`): the verdict bar is capped `max-w-[300px]` (was full-row-width) with 2px gaps between segments so adjacent tones (hold amber / rerun orange) read as distinct blocks instead of one bleeding gradient. **(4)** Agent triage (`AgentTriage.tsx`): the flagged-samples table caps at 10 rows/page + a numbered pager (a large mixed flowcell can flag dozens); selection persists across pages. Verified live: agents open from View; "fingerprint …" + full-value hover; bars capped/gapped; triage shows "1–10 of 17 flagged" with 2 pages. tsc + oxlint clean. Commit `3c6dacb` | Port | done | yes | T-069, T-077, T-078 |
| T-100 | Monitoring: Recharts throughput chart + clear/restore signatures ✅ ("Wave 2") — reworks Monitoring per the maintainer's feedback batch; adds **recharts 3.9.2 (MIT)** as a new `frontend` dependency, justified per the Dependencies guardrail: the hand-rolled SVG bar chart (T-078/T-080) couldn't give hover tooltips + a trend line + a stable frozen frame without reinventing a chart library; React-19-compatible, MIT-licensed, added at the maintainer's explicit request. **Verdict-over-time chart (M1/M2/M3/M5):** now a Recharts `ComposedChart` — stacked per-verdict bars + a monotone "Flagged (trend)" line (hold+rerun+escalate) + a grounded hover tooltip (real per-run counts, no synthesis) + dashed gridlines; FROZEN to a ~14-day column frame (constant per-column slot, `maxBarSize`-capped) that scrolls sideways beyond it — toggling 7d/14d/30d no longer resizes the card. **This REMOVES the per-run pager T-072's frontend half had added** (`34bca5d`) — the chart now renders every fetched run as a bar, scrolling instead of paginating; see the rewritten T-072 row above: the backend `runs[]` payload stays uncapped, and there is now no frontend render-cap either (an honest, not a regression — the maintainer's own call, and a scrolling chart degrades more gracefully than an uncapped table would at today's volume). **Recurring signatures (M4/M6):** each row gains a unique, stable display id — `SIG-<first 8 chars of the signature hash>` — so same-rule patterns (e.g. two PIPE-001s) are distinguishable; and a **clear-from-view / restore** control — a REVERSIBLE, `localStorage`-persisted, per-browser view filter (never a DB purge, never touches `api/`) that moves a signature into a collapsible "Cleared · N" section, still searchable, one click to restore. Verified live (dark + light): chart renders with no console errors, hover shows the real breakdown, 30d scrolls without resizing the card, clear→toggle→restore round-trips and persists across reload. tsc + oxlint clean. Commit `f8a6f35` | Port | done | yes | T-072, T-076, T-080 |
| T-101 | Wave 4: real Submit CSV parsing + `sample_metadata.csv` + API-client error detail ✅ — two commits, closes two long-standing limitations. **`f8d9ea0` — API-client error detail:** `api.ts`'s `get`/`write`/`fetchRunsPage` used to throw a bare `${status} ${statusText}` on failure; a new `httpError()` reads FastAPI's real body (a 4xx `HTTPException`'s `detail` string, or a 422's `detail: [{msg}]` array) and includes it in the thrown `Error` — every off-gate write's error toast app-wide now shows the backend's real reason, no wire-contract change. **`1bb79b8` — Submit rework:** the Upload panel was a visual mock (no `<input type=file>`, a hardcoded "Parsed 4 samples" chip) — `Submit.tsx` now parses for real on drop/browse, tolerant of a missing/renamed column: an **Illumina v2 SampleSheet** (`[Header]` + `[*_Data]`, auto-detecting run name/study/assay/platform) or a **plain CSV** (`Sample_ID,Sample_Type,index,index2,Study`). Adds a **`sample_metadata.csv`** attach (the LIMS/subject sheet, no prior upload path existed) parsing `Sample_ID,Subject_ID,Tissue`, merging tissue into the sample-type column and showing subject id per row — **`subject_id`/`tissue` stay client-side only** (`SubmitRunIn`/`SampleIn` in `api/routers/intake.py` carry no subject field, `extra="forbid"` — persisting it server-side is the next Submit step, gated by the data-platform design's G-PII/G-DEID guardrails, unaffected by this change). Sample-table pagination (25/page + pager) and a scale-aware submit toast (summarize past 5 names, never `join()` 100) keep a large mixed flowcell navigable. `POST /api/runs`'s execution boundary, `HG002`-only fixture scoping, and honest sample-skip behavior (T-057) are **unchanged** — only the input is now real. Verified live with a generated 100-sample mixed DNA/RNA sheet + a 100-row metadata sheet: "Parsed 100 samples," run/study/assay auto-detected, "100 subjects mapped," 4-page pager, and a no-fixture submission honestly 422s with the backend's real message. `git diff --stat e39bb4e 1bb79b8 -- src/ api/ tests/` empty (frontend-only). tsc + oxlint clean. [journal 2026-07-10 wave4](../journal/2026-07-10-wave4-submit-parsing-and-api-errors.md) | Port | done | yes | T-057 |
| T-102 | Audit retrofit: explicit-confirm gate on one-click writes ✅ ("Wave 3") — realizes the maintainer's standing design rule that no single accidental click may trigger a cascading/state-changing write; every off-gate edit must be explicit and audited. New reusable `frontend/src/components/ConfirmDialog.tsx`: a `ConfirmProvider` (mounted at the app root, `App.tsx`, outermost after `ToastProvider`) + `useConfirm()` returning an async `confirm(opts) → Promise<boolean>` (title/body/confirmLabel/cancelLabel/tone `default`\|`danger`; Escape + click-outside both cancel). `ReviewQueue.tsx` routes its stakes-y ticket actions through it: **Resolve/Escalate/Reopen** confirm first, each naming its effect and that it lands in the audit log; **Suppress** uses a DANGER-toned confirm spelling out the cascade ("future occurrences of \<rule\> across runs are hidden until un-suppressed"); **batch Resolve/Suppress** confirm the selected count. **Acknowledge and un-suppress stay direct one-clicks** (low-stakes/non-destructive — consistent with T-097's DC2-part-2 batch-clear framing). `Admin.tsx`'s Act-as (T-092) swaps its native `window.confirm` for the same branded dialog. The actions' backend persistence path is **unchanged** — no new endpoint, no wire change — so they still land in the Admin Activity audit feed exactly as before (REQ-F-063/REQ-F-066/REQ-F-072). Verified live: Cancel aborts with zero effect (Open count unchanged), Confirm fires (decrements), danger tone renders on Suppress, Act-as confirms, audit log intact. `git diff --stat 9733842 d65c9c1 -- src/ api/ tests/` empty (frontend-only). tsc + oxlint clean. Commit `d65c9c1`. [journal 2026-07-10](../journal/2026-07-10-confirm-dialog-audit-gate.md) | Port | done | yes | T-092, T-097 |
| T-103 | Settings: model-tiering → a scale-aware agent table with explicit edit ✅ ("Wave 5," ST1/ST2) — rebuilds `SettingsModelTier.tsx` per maintainer feedback. The old 3-item model-tiering card (dropdowns applied on change) is now a TABLE of the full advisory-agent roster — **Agent · Purpose · Model · Status · Edit** — capped 10 rows/page + pager (the scale-kit pagination pattern, [data-platform-and-archivist.md §4.10](../design/data-platform-and-archivist.md)). Rows: card synthesizer, QC-triage, pipeline-repair, archivist, feedback-categorizer, node-author (roster #5, still design-note-only, T-046), and a **NEW metrics-expansion agent row** (ST2 — proposes new QC metrics to track + wiring; labelled **phase-2**; there is **no backend agent module or `PIPEGUARD_*` env var for it** — confirmed by `grep -rn PIPEGUARD_METRICS_AGENT src/ api/` = no hits — this is a UI-only placeholder for a proposed idea, deliberately **not** added to [design/agents.md](../design/agents.md)'s roster as built). Each row shows its `PIPEGUARD_*_MODEL` env var + model/cost + a Stub·$0/Live status. **Explicit edit** (echoing T-092/T-095's Save/Cancel staging pattern): a pencil opens a staged draft (model + live toggle); nothing applies until **Save** (Cancel discards, verified no leak); one row edits at a time, others lock. A **"New agent"** button crosslinks to `/builder` (the node-author agent's home) — the closest existing authoring surface; a full agent-designer stays a phase-2 seam. **ST2 part 1** (runbook thresholds bound to assay × sample type) was independently verified **already correct** in `SettingsAssayTable.tsx` (per-assay rows, per-sample-type columns, save keyed by the assay slug) — no code change needed. **Still purely client-side** — `saveEdit()` only calls `setRows` (local React state); no `api.ts` call exists in the diff, so the T-045 "UI-only, not wired to `PIPEGUARD_*_MODEL`" gap is **unchanged**, just better-presented. Model tiering remains narration/advice only — verdicts stay rule-derived (ADR-0001). `git diff --stat c79f62c 7b579bb -- src/ api/ tests/` empty (frontend-only). tsc + oxlint clean. Commit `7b579bb`. [journal 2026-07-10](../journal/2026-07-10-settings-agent-table.md) | Port | done | yes | T-045 |
| T-071 | Decision-card QC completeness — the remaining gaps in the card's QC-readout hero, tracked separately from T-068's "not measured" fix — **open.** (a) **Contamination** (verifybamid2) is still not computed for the real-GIAB path — noted since T-017 ("Contamination (verifybamid2) remains") and still true after T-063's full-pipeline run. (b) **Core-side card enrichment** — per-gate sub-verdict rollup, linked-sample, provenance metadata, context-rail fields — flagged as "still owed for the full design, deliberately off this burst" by T-055 and not yet picked up. Note: the T-068 "not measured" placeholder is QC-gate-only **by design**, not a gap — the runbook (`runbook.py`) only carries `qc.*` thresholds, so preflight/variant findings have no numeric threshold to render a placeholder against; they already surface via `GateResultStrip` + cited findings | Port | todo | partial | T-017, T-055 |
| T-072 | Monitoring pagination — `GET /api/monitoring`'s per-run `rows: list[MonitoringRunRow]` has no page/limit — **still open; the frontend mitigation has now been REVERSED, not just narrowed.** `signatures_limit` caps only the ranked signature list (`api/main.py` `get_monitoring`); every kept run in the window still gets a row in the wire payload with no analogous cap, unlike `GET /api/runs` which has `page`/`limit` (T-048) — **the backend `runs[]` payload remains uncapped server-side; this is the one honest open item this row now tracks.** History: (1) commit `34bca5d` (2026-07-10) added client-side pagination of the fetched-but-uncapped `data.runs` array (25/50/100 + pager) — a frontend-only DOM-render cap, not a payload cap. (2) commit `f8a6f35` (2026-07-10, T-100 below, "Wave 2") **REMOVED that pager** and replaced the throughput card with a Recharts `ComposedChart` FROZEN to a ~14-day column frame that scrolls sideways beyond it — per the maintainer, "it scrolls now, so paging the throughput columns made no sense." The new chart renders **every** fetched run as a bar (no client-side cap at all, just horizontal scroll) — so today there is neither a backend nor a frontend cap on `runs[]`; the risk this row exists to track (an unbounded payload/render at real scale) is un-mitigated in either direction, only less visible because a scrolling bar chart degrades more gracefully than an uncapped table would. Stays fine at current volume (29–30 runs, ~14 dated); becomes a real concern once `synthetic/scale.py` (T-050) seeds a much larger window — at that point the fix is server-side (`page`/`limit` on `runs[]`, mirroring `GET /api/runs`), not another frontend re-presentation. [journal 2026-07-10 batch 7](../journal/2026-07-10-builder-modals-and-run-selector.md), [journal 2026-07-10 batch 8](../journal/2026-07-10-batch8-theme-monitoring-recharts.md) | Port | todo | yes | T-048 |
| T-073 | Decision card: honest full three-gate readout (preflight → qc → variant) — the hero previously rendered QC only; preflight/variant were silently absent. Root cause: `GET /api/runbook`'s `RunbookThreshold` carried the numeric threshold *value* in `gate` with no separate pipeline-gate field, so the frontend mistyped that value as the `Gate` enum and `notMeasuredGroup`'s `t.gate === gate` filter never matched. Fix: `api/main.py` `RunbookThreshold` gains `pipeline_gate: Gate` (sourced from the metric registry, `registry.entry(t.our_key).gate`) distinct from the numeric `gate` value; `types.ts` retypes `gate` to `number` + adds `pipeline_gate` + an optional frontend-only `GateReadout.note`; `MetricsPanel.tsx` fixes `notMeasuredGroup` to filter on `pipeline_gate` and adds `emptyGateGroup` — an honest empty-state note for preflight (rule-based, no metric table — see the gate strip/evidence) and variant (no metrics extracted this build), never a fabricated row; `RunDetail.tsx` builds the full preflight→qc→variant skeleton (real projection → runbook not-measured placeholder → honest note). Gate stays byte-for-byte unchanged; no preflight/variant thresholds were added to the runbook (would spuriously flag every card via `rules.py`'s QC-\*-NA path). Verified live on the real GIAB HG002 card (Preflight note → QC rows, all PASS → Variant note); 359/362 backend tests pass, 3 skip (unrelated Postgres-live), no test file changed by this commit. Commit `12ffa30`. **Doc note:** the commit message cites "(T-058)" — that id is already used by the Pipeline-repair agent (done); this row is the correct, non-colliding id for this work. | Port | done | yes | T-045, T-055 |
| T-074 | Top bar: searchable, capped run switcher + F17 real-status-dot fix — the flat run dropdown didn't scale past a handful of runs, and its dot (+ the switcher rows' dots) read `n_attention` rather than the run's real lifecycle `status` (F17: a `running` run with 0 flagged samples showed a green "all clear" dot instead of "Sequencing"). Fix: `verdict.ts` extracts a shared `RUN_STATUS_META` (RunStatus → dot + label; `RunOverview.tsx` now consumes it instead of a local copy); `TopBar.tsx` replaces the dropdown with a search box (filters by run id or platform), a list capped at 8 rows, a "View all runs · N runs →" footer to the Runs list, and an honest "No runs match" empty state; both the pill dot and every row dot now read `RUN_STATUS_META[r.status]`. Verified live: 8 of 29 runs shown, "novaseq" narrows to NovaSeq-platform runs, footer links to all 29. Commit `17a3e56`. **Doc note:** the commit message cites "(T-059)" — that id is already used by the Archivist agent (done); this row is the correct, non-colliding id. | Port | done | yes | T-047, T-064 |
| T-075 | Pipeline builder: germline template instantiates as an EDITABLE draft, not the read-only seeded DAG — "New → From template" previously cleared the canvas and re-showed the hard-coded seeded cards, so the pipeline the demo actually ran couldn't be modified in Edit. Fix: `BuilderShared.tsx` adds `germlineTemplate()`, returning the fastp → bwa-mem2 → samtools markdup → {mosdepth, bcftools call → norm} → MultiQC chain as editable `UserNode[]` + 9 typed `UserEdge[]` (mirroring the seeded `TOOLS`' ids/positions/ports so layout + Validate focus refs still line up; edges resolve port indices by kind so wiring can't forge a mismatched edge); `PipelineBuilder.tsx`'s `newPipeline('germline', …)` now seeds `userNodes`/`userEdges` from the template instead of clearing them, `showSeeded` is gated on `isLinked` (only the ORIGINAL linked pipeline shows the read-only seeded DAG; any new/forked draft shows its editable nodes — terminal Decision gate still always renders), and "Fork to new draft" forks the linked pipeline into an editable template draft. Save now sends the real 7-node graph (was empty for the seeded doc). Verified live: New → From template opens 7 draggable/wireable cards + 9 edges as a "PIPE-FROM-TEMPLATE" draft with Connect/Tidy. Commit `01ba673`. **Doc note:** the commit message cites "(T-060)" — that id is already used by the doc-keeper subagent (done); this row is the correct, non-colliding id. | Port | done | yes | T-044, T-065 |
| T-076 | Monitoring: paginate the recurring-**signatures** list (25/50/100 + pager) — with the GIAB seed there are 50+ recurring signatures and the list rendered them all uncapped (the frontend calls `api.monitoring(window)` with no `signaturesLimit`, so the payload is already uncapped server-side). Ports the Runs-list pagination pattern verbatim into `Monitoring.tsx`: a 25/50/100 `SegmentedControl`, a ‹/numbered/› pager, "Showing X–Y of N signatures", client-side over the already-fetched array, resetting to page 1 on window/search/per-page change. Verified: "Showing 1–25 of 51 signatures", no console errors. Commit `e5d5043`. **Distinct from T-072**, which is the still-open, unaddressed gap in the per-run `rows: list[MonitoringRunRow]` (a different list on the same response) — see T-072's doc note. | Port | done | yes | T-048 |
| T-077 | Provenance: artifact download + full digest + a real QC input ✅ — three maintainer asks on the drill-in, closed with no fabrication. **(1)** Download/open-in-store were honest no-ops (`RunArtifact` had no `url`); added a traversal-hardened `GET /api/runs/{id}/artifacts/{name}` (`FileResponse`; the name must be a bare filename, the resolved path must stay inside the run dir) + a `url` field on `RunArtifact` (`api/main.py`), and `Provenance.tsx`'s download/open anchors now point at it. **(2)** "Is the sha256 a process id?" — no: it is a content digest of the file's bytes (process ids are `arun_…`, ledger event ids `evt_…`); kept the label, added a "show full" toggle revealing all 64 hex chars. **(3)** "Why does QC have no inputs?" — the demux result never crossed to the QC stage; `_ARTIFACT_STAGE` (`api/main.py`) now maps each file to a **list** of `(stage, role)` edges, so `demux_stats.csv` (and `reads`) are demux OUTPUT *and* QC INPUT — the same bytes on two edges, feeding the QC node instead of leaving it orphaned. Also answers sample_metadata-vs-SampleSheet: distinguished by **stage** (intake/input vs demux/input). New test `test_artifact_download_serves_file_and_blocks_traversal` (363 tests total, was 362); tsc + mypy clean. Commit `71a06d6`. | Port | done | yes | T-037 |
| T-078 | Frontend UI refinements batch 4 (four disjoint fixes) ✅ — **(1)** Review queue (`ReviewQueue.tsx`): 25/50/100 pagination + numbered pager (mirrors Monitoring); tickets grouped under a sticky per-run subheader; default filter flipped all → open so resolved tickets leave the active view but stay reachable via the Resolved facet chip. **(2)** Runs card (`RunOverview.tsx`): two-row column layout (identity wraps its own row; the verdict bar spans full width on row 2) so a long `run_id` no longer squeezes the breakdown bar. **(3)** Monitoring (`Monitoring.tsx`): a `DateRangePicker` after the window views (refines the throughput chart client-side) + an honest caption that KPIs/gate-pass stay window-scoped; the stacked bar gains a Y-axis gutter, a "samples" label, and gridline ticks — no charting lib added. **(4)** Agent triage (`AgentTriage.tsx`): the non-scalable pill case-selector becomes a Sample·Verdict·Gate·Headline·Findings table, verdict-ranked, active row highlighted. Verified live (no console errors); tsc + lint clean. Commit `e3e1995`. | Port | done | yes | T-048, T-053, T-062 |
| T-079 | Telemetry: the auto-provisioned Grafana dashboard ✅ — Prometheus (`/metrics` exporter + scrape config) and Grafana (service + datasource) were already wired (T-036); the only gap was a dashboard, so Grafana booted connected but empty. Added `deploy/telemetry/grafana-dashboard.json` (the "PipeGuard — QC decision gate" board: `pipeguard_runs_total`/`pipeguard_samples_total` stat tiles, `pipeguard_cards_total{verdict}` donut + a verdict-over-time trend, `pipeguard_gate_flagged_samples_total{gate}` bar — all four already-shipped series, no new one) + `grafana-dashboards.yml` (provider) + a stable `pipeguard-prometheus` datasource `uid` (`grafana-datasource.yml`) so the dashboard binds deterministically, both mounted into the Grafana service (`docker-compose.yml`) the same way as the datasource. Config only — no app code, no new dependency; still off the offline demo path (docker-compose sizzle). Commit `f696bc7`. | Port | done | yes | T-036 |
| T-080 | Frontend: label the digest "hash" (not the algorithm) + fix the monitoring chart distortion ✅ — two maintainer refinements. **(1)** Defense-in-depth: the Provenance artifact rows + the Archivist-manifest modal now read "hash"/"content hash" instead of naming "sha256" to every viewer; the value + copy + show-full behavior are unchanged (the wire field stays `sha256`, only the UI label changed — no doc claiming "sha256" as the underlying algorithm is now wrong). **(2)** The Monitoring "Verdicts over time" bars stretched/squished as the run count or date range grew; bars are now a **constant width** (28px) inside an `overflow-x-auto` viewport (scrolls sideways when runs overrun the width, fills the row when there are few); the Y-axis gutter stays fixed; the per-gate pass-rate bars were already fixed-count and untouched. Verified live: no "sha256" on screen; the chart viewport is fixed-width + scrollable. tsc + lint clean. Commit `eb7d016`. | Port | done | yes | T-077, T-078 |
| T-081 | Frontend: gate the app behind a demo login + a real admin role ✅ — a login screen now fronts the whole app, with four per-role demo accounts (`l.santos` viewer, `a.rivera` reviewer, `m.chen` approver, `s.ops`/`admin@lab.org` admin; shared password `pipeguard`) so a reviewer can refine each role by signing in as it. Demo-only auth with every production seam labelled inline (OAuth/OIDC, server-side argon2/bcrypt, httpOnly session cookie, real CAPTCHA, signed reset links, TLS) — maintainer chose the demo gate over wiring real auth. New `frontend/src/auth.ts` (roster + synchronous credential check + `localStorage` session — id+role only, no token/password) + `screens/Login.tsx` (email/password, a demo-account picker, a labelled CAPTCHA placeholder gating submit, a generic wrong-credentials message, a "Forgot password?" stub). `RoleContext` gains `session` (the login identity, distinct from `actor`, who the app currently acts as) + `login`/`logout`/`isAuthenticated`/`isAdmin`. **`isAdmin` now follows the LOGIN identity, not "any approver"** — `admin` is a frontend-only governance capability (`ADMIN_IDS`, derived from the roster) layered over the wire roles (viewer/reviewer/approver): an admin is an approver who *also* holds governance, distinct from a plain approver — this **corrects** the REQ-F-066/architecture.md framing from the T-066 landing ("visible only when the acting RoleContext is approver"), which is no longer accurate. `App.tsx` wraps every route in a `RequireAuth` guard (redirects to `/login`, preserves the intended destination); Sidebar's dead "Sign out" now logs out; Admin nav + the `/admin` route both gate on `isAdmin`. Verified live: protected route → `/login`; CAPTCHA gates submit; admin sign-in → Admin nav; sign-out → `/login`; redirect-to-intended-destination works. tsc + lint clean. Commit `0f7e85f`. | Port | done | yes | T-052, T-066 |
| T-082 | Core: enrich contrived QC to a full three-gate readout ✅ (+ same-day follow-up) — the decision card only ever carried the frozen-five QC metrics, so Preflight and Variant were always empty notes even for a "rich" run; maintainer chose "enrich demo runs" over leaving them permanently empty. Gate-safe by construction (no result fabricated, verdicts unchanged): `models.QCMetrics` + `parsers.parse_qc_metrics` + `metrics/mapping._QCMETRICS_MAP` gain **8** additional REGISTERED metrics (`preflight.phix_aligned`; `qc.breadth_20x`/`breadth_30x`/`pct_mapped`/`on_target`; `variant.dp`/`gq`/`titv`), each with its declared raw unit, `None`-tolerant so a 5-column run is unchanged. `runbook.QCThreshold` gains `required` (default `True`); **5 new OPTIONAL thresholds** (breadth_20x/30x, pct_mapped, on_target, variant.dp — concrete gate/hard_fail values in `runbook.py`) score a value that IS present but never NA-flag a run that omits it (`rules._evaluate_metric` skips the NA finding when `not threshold.required`); phix_aligned/variant.gq/variant.titv stay **ungated observations** (registered, mapped, ranked in the readout, but no runbook threshold). The metric catalog is now **10 gated / 10 ungated** (of 20 total registered `our_key`s) — of the 10 ungated, only the 3 above are actually populated from `QCMetrics`; the other 7 (`zero_cov_targets`, `fold_enrichment`, `fold_80`, `identity.ngscheckmate_match`, `identity.sex_concordance`, `contamination.freemix`, `variant.allele_balance`) remain registered-only with no parser yet (unchanged, honest gap). The synthetic generator emits all 8 (comfortably passing; failure modes stay on the frozen five) — `mock_run_02/03`/`scale_30` regenerated. **Same-day follow-up** (`a9b06ad`, "T-082 follow-up"): `api/card_readout.py` now labels an ungated row with the registry's **display name** ("Genotype quality (GQ)", "PhiX aligned", "Ts/Tv ratio") instead of the raw `our_key` (registry loaded once + cached); `scripts/run_giab_pipeline.py` now writes `breadth_20x`/`breadth_30x` from mosdepth into `qc_metrics.csv` (previously computed, never written), so the **real** HG002 card's QC group is honestly richer (99.24%/97.07%, both PASS) without contriving anything; `cluster_pf` stays blank (a run-level SAV metric a fastq→BAM path can't produce). `scripts/fetch_giab_hg002.py` noted as manifest-driven, so an HG003/HG004 trio sample is fetchable by pointing `--manifest` at a new accession file. 363 tests green (was 362 pre-T-077; unchanged by T-082 itself); ruff + mypy clean. Commits `a8fc73b` + `a9b06ad`. | Port | done | yes | T-024, T-025, T-055, T-073 |
| T-083 | Pipeline builder: fix the broken connector lines + the tool I/O ✅ — the seeded DAG's connectors were hardcoded SVG path strings anchored to fixed pixels, so they detached from the ports the moment a card's port count changed (the "broken lines" report), and several tool nodes had wrong I/O (the "connections don't make sense" report) — both fixed, grounded in the real pipeline (`scripts/run_giab_pipeline.py`). **Tool I/O corrected:** bcftools call gains a `panel_bed` input (`mpileup -R PANEL`); bcftools norm loses it (norm has no `-R`); samtools markdup now outputs `bam`·`bai`·`markdup_metrics` (was a phantom `samtools_stats` with no producer); bwa-mem2 outputs `bam` only (the `.bai` comes from markdup's index, not bwa); mosdepth gains `mosdepth_thresholds`; MultiQC's fan-in drops the phantom `samtools_stats`. `BuilderShared.tsx`'s `TOOLS`/`BTOOLSPEC`/`germlineTemplate` all corrected together (palette subs match). **Broken lines fixed:** the seeded edges are now **COMPUTED** from the tool/reference card geometry + typed ports (`BuilderCanvas` `SEEDED_WIRES`/`REF_WIRES` + a `toolAnchor` helper) — the same anchor scheme the user-composed edges already used — so a connector always lands on its port and can never detach; the old hardcoded `EDGES` table is removed. Verified live: 8 tool→tool + 5 ref→tool computed edges render as clean elbows, no console errors; bcftools call shows `reference_fasta` + `panel_bed` inputs. tsc + lint clean. Commit `d8c1625`. | Port | done | yes | T-044, T-063 |
| T-084 | Pipeline builder: Fit-to-DAG, trackpad zoom, bigger minimap ✅ — three canvas-navigation asks. **Fit** now snaps the viewport onto the pipeline (centers the seeded DAG, or the composed nodes' bounding box when authoring a draft) and resets zoom, instead of only resetting the zoom level. **Trackpad/mouse zoom**: a ctrl-wheel (what a pinch and Ctrl+scroll both emit) now zooms the canvas instead of the whole page — attached natively with `{ passive: false }` since React's `onWheel` is passive; plain wheel still pans; clamped to the same 0.6–1.4 range as the zoom buttons. **Minimap** grew from a 168×46 strip to a 210×108 proportional mirror of the whole canvas (uniform scale, no x/y distortion), rendering the seeded tool cards, references, the terminal gate, and any composed nodes to scale. Verified live: minimap is 210×108 with 11 scaled nodes; tsc + lint clean, no console errors. Commit `07f53af`. | Port | done | yes | T-044, T-083 |
| T-085 | Pipeline builder: connection-preserving Tidy, Cancel a draft, minimap top-right ✅ — batch-5 builder-canvas asks. **Tidy** is now a flow-preserving auto-layout: each node's longest-path depth from a source is relaxed over `userEdges` (`|V|` passes), then placed in the column of its depth (`60 + col*230, 56 + row*120`) with parallel nodes stacked in a column — upstream→downstream reads left→right, instead of the old one-row `150 + i*170` layout that lost the connection structure. **Cancel** (shown only while composing a draft, `!isLinked`) resets every draft-scoped state var and returns to the linked pipeline in View — an explicit exit that was missing. **Minimap** moved `bottom-right`→`top-right` (no longer sits under the feedback bubble); already stopped mirroring the seeded squares on a new build since T-084 gated the seeded layer on `isLinked`. tsc + lint clean (types + code review; Chrome extension disconnected this stretch). Commit `14c9f3c`. | Port | done | yes | T-044, T-084 |
| T-086 | Pipeline builder: reference SOURCE cards + collapsible palette ✅ — fills the "no way to add bed/vcf/reference cards" gap. `BuilderShared.tsx` `BTOOLSPEC` gains three no-input entries (Reference FASTA → `reference_fasta`, Panel BED → `panel_bed`, Truth VCF → `truth_vcf`); a new **References** palette section wires them to the existing `addNode`, so an operator drops a reference and Connects it into a tool's typed ref input (a fasta can't land on a fastq port). The palette's sections are now **collapsible** (chevron + per-section item count); an active search query overrides collapse so matches always show. tsc + lint clean (types-verified; live browser check still unavailable). Commit `c6a6210`. | Port | done | yes | T-044, T-085 |
| T-087 | Decision card: QC gates downstream — a hold blocks the variant gate ✅ (DC2 part 1) — the maintainer's two-tier gate-dependency model: sequencing-tier QC (preflight) gates sample processing, sample-tier QC gates downstream analysis. `api/card_readout.py`'s `GateReadout` gains `blocked_by: Gate \| None`; `_blocking_gate()` walks upstream from a gate and returns the nearest gate in `unclear = {gr.gate for gr in card.gate_results if gr.verdict is not PROCEED}` (a gate only carries a `gate_result` when it has findings, so "has one" ⟺ "not clear"). A gate whose upstream isn't clear now reads "blocked · clear \<upstream\> first" instead of "all clear," so a QC hold no longer looks like the sample proceeded to variant calling. Frontend: `types.ts`/`MetricsPanel.tsx` `ReadoutGroup` gain optional `blocked_by`; the `Rollup` pill renders it ahead of "all clear"/`not_measured`; `RunDetail.tsx`'s `CardBody` mirrors the same nearest-upstream-unclear computation client-side for the placeholder/empty gate groups it synthesizes. **Pure re-presentation — no verdict change** (the card's verdict already reflects the QC finding; ADR-0001 intact, confirmed `rules.py`/`synthesis/` untouched). New test `test_qc_hold_blocks_the_downstream_variant_gate` (`tests/test_card_readout.py`, now 13 cases): `mock_run_02` sample S3 (QC hold) → Variant `blocked_by=qc`; a fully-clear sample blocks nothing. 364 tests total (was 363); ruff + mypy + tsc + lint clean. Part 2 (user-clearable HOLD/ESCALATE, individually + in batches) is the next slice, not yet a task row. Commit `545c893`. | Port | done | yes | T-055, T-073 |
| T-088 | Decision card: drop the redundant left verdict color bar ✅ (DC3) — removed the 3px verdict-colored left spine from `RunDetail.tsx`'s decision cards (`VERDICT_STRIPE` import + its `className` dropped); the verdict is already carried by the card's badges/pills, so the bar was redundant. Per the maintainer, the colored left rail is reserved for the Pipeline-Builder tool cards (`BuilderCanvas`, unaffected — keeps its own spine to differentiate tools). The card keeps its neutral border. tsc + lint clean (code-verified). Commit `24940e1`. | Port | done | yes | T-055 |
| T-089 | Decision card: green "Passed" gate pills in the top strip ✅ (DC1) — `GateResultStrip.tsx`'s no-gate-result chip is now conditional on `blocked` (card verdict escalate/rerun): unblocked renders the shared proceed/green token set (`border-proceed-bd bg-proceed-bg text-proceed-fg`) instead of a neutral grey, so a clean gate reads as clearly passing across the preflight/qc/variant row; a hard-blocked-upstream gate keeps the neutral grey "Not run." Distinct component/pill from T-087's `blocked_by` (this is the top-strip chip driven by the card's own verdict, not the QC-readout hero's gate-dependency chip). Verified live: "Passed" chips render green. tsc + lint clean. Commit `d5fdcb2`. | Port | done | yes | T-055 |
| T-090 | Provenance: view an artifact on name-click, download only from the button ✅ (P1/P2) — `GET /api/runs/{id}/artifacts/{name}` (`api/main.py`) gains a `download: bool = False` query param; `FileResponse(..., content_disposition_type="attachment" if download else "inline")` (traversal-hardening untouched). Name-click now views the artifact at its location (`href=art.url`, no change); the separate Download anchor now requests `` `${art.url}?download=1` ``. **P2:** a name-sensitive hover title on `sample_metadata.csv` ("Intake · LIMS/subject metadata sheet") vs `SampleSheet.csv` ("Demux · Illumina barcode/index manifest") surfaces a distinction that previously lived only in the stage grouping. `tests/test_api.py` extended (not a new test function) to assert `inline` by default and `attachment` on `?download=1`. Verified; tsc + mypy + lint clean. Commit `de5fa94`. | Port | done | yes | T-077 |
| T-091 | Frontend: real, persisted theme + density preferences ✅ — the Settings dialog's Theme/Density controls were local state wired to nothing. New `frontend/src/context/PrefsContext.tsx` (theme light/dark/system, density split/brief/dense; `localStorage` key `pipeguard.prefs`, `try/catch`-guarded load/persist). Theme resolves `system` via `matchMedia` and stamps `document.documentElement.dataset.theme`, following the OS live while on `system`. A full dark theme in `index.css` (`:root[data-theme="dark"]` overrides the `@theme --color-*` vars: page/card/surfaces/text/accent + dark verdict bg/border/fg + shadows), so every existing Tailwind utility retargets with no per-component change. `App.tsx` wraps the tree in `PrefsProvider`; `UserSettingsDialog.tsx` and `RunDetail.tsx`'s card-density control now read/write the same saved density — one setting, survives across runs + refresh. Verified live: dark → body #0f151b / cards #171e27, readable chips/pills; density=brief applies the brief layout. tsc + lint clean. Commit `08a42ad`. | Port | done | yes | — |
| T-092 | Admin: role edits stage into a draft + Save/Discard; dropdown; confirm Act-as ✅ (A1) — roles previously changed on every click of a 3-way toggle (a stray click reassigned a role). `Admin.tsx`'s `UsersTab` gains a `draft: Record<string, Role>` staging map (`stage()` adds/removes an entry, clearing when it matches the saved role) and a `dirty`-gated Save/Discard bar; the role control is now a native `<select>` (Viewer/Reviewer/Approver) with an "unsaved" badge while staged. `save()` applies every staged role and re-syncs the live actor only if its own role was staged. `actAs(u)` now `window.confirm()`s before impersonating, naming the target + role. Touches only the **client-mock** roster (`api/auth.py` unchanged; [RISK-035](../quality/risks.md) already covers the localStorage-bypass posture — this hardens the legitimate UI path, not the security boundary). Verified live (as admin): 4 role dropdowns, staging shows "unsaved" + the Save/Discard bar. tsc + lint clean. Commit `5774143`. | Port | done | yes | T-052, T-066 |
