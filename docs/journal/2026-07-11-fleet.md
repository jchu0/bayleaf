# Journal — 2026-07-11 (MST) — Fleet: node-author W2 backend + a11y sweep + reliability tests

| Field | Value |
|---|---|
| **Focus** | Doc-keeper SWEEP + AUTHOR + CHK over three already-committed fleet commits: `5a3dd6a` (node-author W2 backend), `4427ec2` (a11y), `63b87ce` (reliability tests) — ground every claim in the committed code, update every doc the Doc-update map obligates, do not touch code. |
| **Participants** | James Hu (maintainer, commits authored earlier), Claude Code (doc-keeper subagent, this pass) |
| **Outcome** | Nine canonical docs updated (ADR-0016, architecture.md, node-authoring-agent.md, agent-authoring-contract.md, agents.md, functional.md, nonfunctional.md, ui-conventions.md, schemas.md, scope-and-wishlist.md, evaluation.md, tasks.md, TABLE_OF_CONTENTS.md — 13 total); census refreshed to 555/41, 549 pass/6 skip; the CLAUDE.md code-map row is caller-owned (guardrail — this subagent cannot edit CLAUDE.md). |

## Discussion

### Grounding pass

Read all three commit diffs in full (`git show <sha>`) before touching any doc, per the doc-keeper
contract's "ground every claim in code" invariant:

1. **`5a3dd6a`** — `api/library_store.py` (new, 244 lines: `LibraryStore` Protocol +
   `JsonlLibraryStore`/`SqliteLibraryStore`, `PIPEGUARD_LIBRARY_STORE=jsonl|sqlite`, degrade-to-JSONL,
   deliberately no Postgres adapter — mirrors `api/job_store.py`'s shape exactly). `api/routers/
   node_author.py` grew a `POST /api/builder/node-proposal/accept` (`reviewer`/`approver` via
   `require_role`, `extra="forbid"` on the accept body so a client sends only `request` and the
   server RE-DERIVES the proposal via `propose_node()` — this is the load-bearing safety property, a
   caller cannot author library metadata directly) + `GET /api/builder/library`. New
   `src/pipeguard/node_author/conformance.py` (220 lines): `check_conformance()` is pure/
   deterministic, mechanically asserts five capability pins (advisory-True; no `verdict`/
   `confidence` key anywhere, scanned recursively by KEY not value so `generated_by: "stub"` is
   never a false positive; no `script`/`stub` command-body key anywhere; closed port vocabulary with
   unknown→`reserved`, including a check that a declared-`reserved` kind isn't secretly real; four-
   way versioning). New `src/pipeguard/node_author/importer.py` (230 lines):
   `import_from_nextflow_schema()` deterministically walks an nf-core schema's `definitions`/`$defs`
   groups, maps `format: file-path` params to ports via a conservative ordered hint table
   (`_KIND_HINTS`), falls through to a `reserved_`-prefixed slug on no match — verified this can
   never alias a real `ARTIFACT_KINDS` member. Read all four new test files (34 tests total) to
   confirm every claim in the module docstrings is actually asserted, not just stated — e.g. spot-
   checked `test_accept_rejects_a_smuggled_extra_field`, `test_verdict_and_confidence_fields_are_
   flagged_at_any_depth`, `test_unknown_kind_is_reserved_never_invented_as_a_live_wire`.
2. **`4427ec2`** — read the diffs for `Tabs.tsx`/`Pager.tsx`/`SegmentedControl.tsx`/
   `RunSelector.tsx` directly: roving-tabindex + arrow/Home/End on Tabs, a `nav aria-label`
   landmark + `aria-current`/`aria-label` on Pager, `role="radiogroup"`/`radio` +
   `aria-checked` on SegmentedControl, the full combobox/listbox pattern on RunSelector
   (`aria-activedescendant`, arrow-key highlight, Enter-to-pick). The commit body's contrast-
   verification claim ("all 8 fg/bg pairings pass WCAG AA 5.5–9:1") could not be independently
   re-run in this pass (no browser tooling available to a doc-only subagent) — it is reported as
   the commit's own stated verification, attributed as such, not re-derived here.
3. **`63b87ce`** — read the full `test_job_store.py` diff (170 new lines): the `killpg` live-reap
   test spawns a real `sh -c "sleep 30 & echo $!; wait"` grandchild through `run_driver` with a
   monkeypatched 1s `DRIVER_TIMEOUT_S`, and asserts the grandchild PID is dead after the timeout;
   the negative control stubs `job_store._kill_group` down to `proc.kill()` (direct-child-only, the
   pre-fix shape) and asserts the SAME grandchild SURVIVES — this is what makes the positive test's
   assertion non-vacuous, and is worth calling out explicitly in the evaluation.md rewrite. Both are
   `skipif`-gated on POSIX process groups + `/bin/sh` presence. The atomic dup-run-id test uses a
   real `threading.Barrier` + two threads hitting the actual router function
   (`intake.submit_run`), not a re-implementation, with the background driver thread stubbed to a
   no-op so the winner never shells out to Nextflow — confirms the "two concurrent submits" claim is
   about genuine thread contention, not a mocked serialization.

### Doc-update map sweep

Walked the ToC's Doc-update map against what the three commits touched (`src/pipeguard/node_author/`,
`api/library_store.py`, `api/routers/node_author.py`, four `frontend/src/components/*.tsx` +
three `frontend/src/screens/*.tsx`, `tests/test_job_store.py`). Fired rows, in order applied:

1. **New advisory-agent-adjacent capability + a new pluggable store** → `ADR-0016` (new item 9,
   Costs/Follow-ups updated to reflect the store not adding an 8th Postgres dialect — verified via
   `grep -rln "class Postgres" api/ src/` returning the same six files as before, `api/
   library_store.py` confirmed absent from that list).
2. **`api/` endpoint + capability change** → `design/architecture.md` (Swappable-seams table row +
   a new library-store row + a history bullet under the item-4 narrative).
3. **Agent-authoring-adjacent design docs** → `design/node-authoring-agent.md` (Status header,
   "What actually shipped" item 6, "Next slices" reworked with strikethrough on the now-done items)
   + `design/agent-authoring-contract.md` (Status header, moved items 1–4 of the old "Deferred" list
   into a new "Wired, backend-only" tier with honest narrowing notes on each — e.g. accept does NOT
   ride the `PipelineGraph`/`pipelines_lifecycle` machinery the doc originally sketched, it has its
   own store; `check_conformance()` is a per-candidate function, not yet the roster-wide CI sweep the
   doc's item 4 envisioned) + `design/agents.md` (roster row #5 extended).
4. **REQ rows** → `requirements/functional.md` (REQ-F-096 for the backend accept/library/
   conformance/importer bundle, REQ-F-097 for the a11y extension, REQ-F-025 narrowed, Notes item 7
   rewritten) + `requirements/nonfunctional.md` (REQ-NF-070 addendum item 2) — a coupled cluster,
   updated together per the map's own "coupled clusters" note.
5. **A durable, cross-cutting UI convention extended** → `design/ui-conventions.md` (UIC-19 title
   + body extended, a new "Extended" intro paragraph).
6. **`src/pipeguard/models.py`-adjacent contract** → `data/schemas.md` (`LibraryEntry` added as
   Persistence item 7, mirroring item 6's job-store framing).
7. **Scope/wishlist "built" change** → `requirements/scope-and-wishlist.md` (items 9 and 11 both
   needed a correction — item 9's importer claim is real but narrower than "schema→form"; item 11's
   "accept→card stays deferred" needed splitting into "backend built / UI button deferred").
8. **Test census + EVAL-008 currency** → `quality/evaluation.md`: recomputed the census with
   `uv run pytest --collect-only -q` (555 collected) + `git ls-files 'tests/*.py' | wc -l` (41),
   confirmed `uv run pytest -q` → `549 passed, 6 skipped` locally (this sandbox has no `nextflow` on
   `PATH`, confirmed via `which nextflow`), rewrote the per-file breakdown line (arithmetic
   double-checked: the 41 file counts sum to exactly 555), rewrote EVAL-008's Method/Known-failure-
   modes to describe the now-live `killpg` test + its negative control (closing the "not covered by
   an automated test" line the case used to carry for that one behavior), and authored a new
   EVAL-014 for the W2-backend bundle.
9. **Task board** → `planning/tasks.md`: three new rows, T-135/T-136/T-137, one per commit, in the
   established per-commit granularity this board already uses; updated the header `Last updated`.
10. **ToC row currency** → `TABLE_OF_CONTENTS.md`: updated the `node-authoring-agent.md` and
    `agent-authoring-contract.md` one-line descriptions, and ADR-0016's row (five→seven pluggable
    stores, naming both no-Postgres-by-design ones).

### Waived rows (named, per CHK-2)

1. **`runbook.py`/`rules.py` (qc_metrics.md row)** — waived. None of the three commits touch a
   threshold, a metric, a gate assignment, or verdict policy; `git show --stat` on all three
   confirms no file under `src/pipeguard/runbook.py`/`rules.py` appears.
2. **`src/pipeguard/metrics/` (metric_registry.md row)** — waived. No metric-registry file touched.
3. **`provenance.py`/`engine.py`/`EventType` vocabulary row** — waived. No new `EventType`, no
   ledger-format change; the library store is explicitly modeled as product state, never a
   provenance event (see `data/schemas.md` item 7's own wording).
4. **`ops/telemetry-connectors.md` (the `/metrics` exposed-series sub-clause)** — waived. No
   `_render_prometheus`/`/metrics` change in any of the three commits.
5. **`demo/*` / `README.md` (demo-flow/exact-commands row)** — waived. No demo script, port, or
   run-of-show step changed; the new endpoints are backend-only with no frontend caller, so the
   live demo path is byte-identical.
6. **`data/licensing.md`/`constraints.md` (new dependency row)** — waived. No new third-party
   dependency was added by any of the three commits (`git show --stat` on all three shows no
   `pyproject.toml`/`package.json` change).
7. **`CLAUDE.md` code-map row** — explicitly **caller-owned**, per this task's instruction and the
   agent's own permission guardrail (doc-keeper cannot edit `CLAUDE.md`). Not attempted here.

### Honesty checks performed before writing each claim

- Confirmed via `grep -rn "node-proposal/accept\|builder/library" frontend/src` (empty) before
  writing "no frontend caller yet" anywhere — this claim recurs across five docs and was verified
  once, not asserted five times on faith.
- Confirmed via `grep -rln "class Postgres" api/ src/` (six files, `library_store.py` absent)
  before writing the ADR-0016 Costs-row update.
- Confirmed via `grep -n "PIPEGUARD_LIBRARY\|PIPEGUARD_JOB_STORE" .env.example` that both var
  families are genuinely present before citing the `.env.example` hygiene fix.
- Recomputed the EVAL-014 test-name citations against `grep -n "def test_"` on all four new test
  files rather than trusting the commit-message prose.

## Decisions

No new load-bearing architectural decision was made in this pass — it is a documentation sweep
over already-committed, already-decided code. No new ADR was authored; CHK-3 is N/A this session
(see below).

| Decision | Distilled to |
|---|---|
| N/A this session — doc sweep only, no new design decision | — |

## Open questions & TODO

- The Builder's own "Accept to library" button (frontend caller for the new accept/library
  endpoints) — tracked as a named deferred slice in `agent-authoring-contract.md`,
  `node-authoring-agent.md`, `scope-and-wishlist.md` #11, and `functional.md` REQ-F-096.
- The `draft→approved` library-entry transition — same set of docs.
- The free-text `--help`/README half of the doc-drop importer — its own spike, per the module's
  own docstring.
- A roster-wide, CI-parametrized `check_conformance()`-style sweep across all six advisory agents
  (today's harness runs against node-authoring candidates only) — named in
  `agent-authoring-contract.md`'s deferred list, item 4.
- The a11y contrast-verification claim in `4427ec2`'s commit body was reported here as-is
  (attributed to the commit), not independently re-measured — a future session with browser
  tooling could re-verify it directly rather than trusting the commit message.

## Distilled into

- [docs/adr/ADR-0016-postgres-port.md](../adr/ADR-0016-postgres-port.md) — new item 9, Costs/
  Follow-ups updated.
- [docs/design/architecture.md](../design/architecture.md) — Swappable-seams table + history bullet.
- [docs/design/node-authoring-agent.md](../design/node-authoring-agent.md) — Status header, item 6,
  Next-slices.
- [docs/design/agent-authoring-contract.md](../design/agent-authoring-contract.md) — Status header,
  templates table, Status section rewritten (Wired backend-only tier + narrowed Deferred tier),
  Capability-pins table.
- [docs/design/agents.md](../design/agents.md) — roster row #5.
- [docs/requirements/functional.md](../requirements/functional.md) — REQ-F-025 narrowed, new
  REQ-F-096/REQ-F-097, Notes item 7 rewritten.
- [docs/requirements/nonfunctional.md](../requirements/nonfunctional.md) — REQ-NF-070 addendum.
- [docs/design/ui-conventions.md](../design/ui-conventions.md) — UIC-19 extended.
- [docs/data/schemas.md](../data/schemas.md) — new `LibraryEntry` Persistence item 7.
- [docs/requirements/scope-and-wishlist.md](../requirements/scope-and-wishlist.md) — items 9 and
  11 corrected.
- [docs/quality/evaluation.md](../quality/evaluation.md) — census refreshed (555/41, 549 pass/6
  skip), EVAL-008 Method/Known-failure-modes rewritten, new EVAL-014.
- [docs/planning/tasks.md](../planning/tasks.md) — new rows T-135/T-136/T-137, header updated.
- [docs/TABLE_OF_CONTENTS.md](../TABLE_OF_CONTENTS.md) — two design-doc row descriptions + the
  ADR-0016 row, header updated.
