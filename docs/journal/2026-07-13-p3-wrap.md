# 2026-07-13 (MST) — P3 wrap: node-author scaffolds-as-assets + P3 close-out

**Topic:** Final P3 increment (scaffolds-as-assets) + wrapping P3 (per-agent capability upgrades,
design/agent-capabilities.md) with an honest built-vs-deferred accounting.

## Final increment (`2500d4b`) — scaffolds-as-assets (§3.4)

`node_author/scaffolds.py::render_scaffolds(proposal)` renders filled **DRAFT** artifacts from a
node-author proposal — a `ProcessSpec` catalog-entry skeleton + a Nextflow `process` skeleton (+ a
metric-registry entry when the tool emits QC), with ports/names wired from the corpus and the
runnable command left an explicit `TODO`. The agent lays out a conform-by-construction skeleton; a
HUMAN authors the compute (the agent NEVER writes `script:`/`stub:` — compose ≠ execute,
ADR-0001/0003). Surfaced read-only via `GET /api/builder/node-proposal/scaffolds` + a "Starter
scaffolds" collapsible in the Builder modal. 4 offline tests; live-verified (fastp → 3 files, fastq
input wired, command a TODO).

## P3 — built this session

| § | Built | Commits |
|---|---|---|
| §1 | Archivist read-only cross-run DB retrieval, grounds the chat | `8838142` |
| §2 | Pipeline-repair bayleaf-system + tool **docs corpus** (chat grounding) | `08beb3f` |
| §3 | Node-author **Accept-to-library** button + **scaffolds-as-assets** | `96b363b`, `2500d4b` |

All advisory (ADR-0001), stub-first (ADR-0006), structured/de-identified where they touch data.

## P3 — deferred (honest, with reasons)

1. **§2 issues+resolutions store** — its PRODUCER is the monitoring tool-agent (ADR-0023), not
   built. The docs-corpus half shipped; this half waits on that agent.
2. **§3 doc-upload → schema-delta-as-proposal** — the `nextflow_schema.json` importer exists
   backend-only; wiring an upload that drafts card + Nextflow wiring + a human-approved schema delta
   is a distinct, larger piece (touches the core data contract — must stay a proposal, never
   auto-mutate).
3. **§3 inline-edit before accept + `draft→approved` transition** — the store carries `status`; the
   transition rides the same audited flow the pipeline lifecycle uses when it lands.
4. **§3 persist scaffolds as versioned assets (ADR-0025)** — rendered on demand today, not stored.

## Session-end doc checklist (DOCUMENTATION_HABITS §Session-end)

- **CHK-1 (journal):** ✅ this entry + the per-increment journals
  (`2026-07-13-archivist-retrieval-build.md`, `-node-author-accept-to-library.md`).
- **CHK-2 (doc-update map):** ✅ `agent-capabilities.md` (§1/§2/§3 statuses), `TABLE_OF_CONTENTS.md`
  (row + statuses) updated in the same change as the code.
- **CHK-3 (crosslinks):** ✅ Related fields + inline ADR links resolve (ADR-0001/0003/0023/0024).

## Verification

`make check` green across P3 (final full suite **772 passed / 8 skipped**, mypy 102 files, ruff
clean); frontend `tsc -b` + vite + lint clean. Each agent capability live-verified over the API
(archivist cross-run aggregate; repair system-doc grounding; node-author accept → draft entry +
scaffolds).

**Related:** [design/agent-capabilities.md](../design/agent-capabilities.md) ·
[ADR-0023](../adr/ADR-0023-agent-taxonomy-and-action-boundary.md) (the deferred §2 producer) ·
[ADR-0024](../adr/ADR-0024-scope-by-wiring.md) · [ADR-0025](../adr/ADR-0025-versioned-reversible-agent-config.md) ·
[TABLE_OF_CONTENTS.md](../TABLE_OF_CONTENTS.md)
