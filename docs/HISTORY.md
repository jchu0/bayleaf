# bayleaf — Build History (archive)

| Field | Value |
|---|---|
| **Status** | Archive — chronological build narrative, **not loaded each session** |
| **Last updated** | 2026-07-12 (MST) |
| **Audience** | anyone reconstructing *when/how* a capability was built |
| **Related** | [TABLE_OF_CONTENTS.md](TABLE_OF_CONTENTS.md), [journal/](journal/), the ADRs in [adr/](adr/) |

## What this is (and is not)

This file holds the **dated, wave/batch-by-batch build chronology** that used to live inline in
the canonical docs (ADRs, `data/` docs). It was relocated here so the canonical docs stay
**small-and-current**: they now carry the *decision, the why, and the CURRENT status* (built vs
deferred), and point here for the play-by-play.

1. **Not the source of truth.** The canonical docs and the code are. This is context: *when* a
   thing landed, the commit it landed in, and the honest limits recorded at each step.
2. **Git is the real archive.** Full detail lives in `git log` and the dated `docs/journal/`
   entries; this file is the curated index into that history.
3. **Append-only in spirit.** Add a new dated section when a canonical doc sheds chronology;
   don't rewrite past sections.

---

## ADR-0003 — Nextflow becomes executable (deployment-agnostic ports)

Canonical decision + current status: [adr/ADR-0003](adr/ADR-0003-deployment-agnostic-ports.md).
Design detail: [design/nextflow-codegen.md](design/nextflow-codegen.md).

### 2026-07-08 — ports built with local adapters

1. Event bus = `EventLedger` (in-memory + JSONL, `provenance.py`, ADR-0002); run store = the
   `Repository` protocol (`persistence/repository.py`) with a `SqliteRepository` adapter **and** a
   guarded OFF-by-default `PostgresRepository` (ADR-0016), + a `rebuild-db` replay targeting either
   backend; notify = the `NotifyPort` protocol (`notify/`) with stub + Slack + Teams + Discord
   adapters (ADR-0010); **artifact store = the `ArtifactStore` protocol** (`artifacts/`, T-039)
   with a zero-dep `LocalArtifactStore` and an OFF-by-default `S3ArtifactStore`. Each flips at the
   edge, never from the core.
2. Metric registry is a versioned-artifact seam (`metrics/`) the rules read through — not a
   hardcoded table.
3. Artifact-store port (T-039) is a materialize-to-local boundary UPSTREAM of the gate: `fetch(run_ref)
   -> local Path`; the unchanged `load_run` then reads that dir, so the store LOCATES a run's bytes
   and never influences a verdict (ADR-0001). `S3ArtifactStore` mirrors the notify seam's safety
   shape: `boto3` is a lazy optional `[s3]` extra; the live pull is opt-in behind
   `BAYLEAF_S3_LIVE`; ANY error degrades to the local store. The 7 other data-platform connectors
   (wishlist #13) implement this same port but are deferred (each needs its own SDK + auth +
   fixtures; the warehouses need a query→artifact adapter shape).
4. Still wishlist at this point: the **job runner** and the **cloud/Slurm compute adapters +
   Terraform**.

### 2026-07-09 — a narrow, demo-scoped job-runner instance

`api/routers/intake.py` (T-057, commit `e77c2e6`) adds `POST /api/runs`: an in-process job registry
(dict + lock, no queue/retry/durability) that triggers `scripts/run_giab_pipeline.py` — at the
time, a **bioconda-toolchain driver, not Nextflow** — as a background subprocess, then exposes `GET
/api/runs/{id}/intake-status` to poll it. First time the API layer **triggers** an external pipeline
run rather than only composing config for one; the core invariant is unchanged (`src/bayleaf/`
never runs a tool). Not the wishlist job-runner *port* (no adapter abstraction, one hard-coded
script, HG002-fixture-scoped). **Superseded 2026-07-11** — the "bioconda-toolchain driver, not
Nextflow" line no longer holds; the driver now runs the same toolchain *through* Nextflow.

### 2026-07-11 — Nextflow becomes executable: codegen + a Nextflow-first driver

1. **A card-graph → Nextflow compiler, `src/bayleaf/nextflow/`** (T-123, commits
   `10f1816`/`be69d6b`). Pure text codegen — emits `main.nf`/`modules/*.nf`/`nextflow.config` for a
   typed Builder graph and never invokes a tool. A curated `catalog.py` (the 7 germline-chain tools:
   bioconda + biocontainer packaging, typed ports, a real `script:` AND a `stub:`) backs
   `compiler.py`'s `compile_graph()`; an uncatalogued tool compiles to a labelled placeholder that
   fails loudly on a real run, never a fabricated command. The seeded chain compiles to the
   committed reference pipeline (`pipelines/germline/`, regenerated by
   `scripts/generate_reference_pipeline.py`), pinned byte-for-byte by a drift test. Exposed by
   `POST /api/pipelines/compile` (JSON preview or `.zip`) + a Builder "Export to Nextflow" UI.
2. **The intake driver is now Nextflow-first** (T-123, commits `8f9d527`/`e4ba174`).
   `scripts/run_giab_pipeline.py` runs `nextflow run pipelines/germline/main.nf` via `subprocess.run`,
   then parses the published QC outputs into the frozen-five run dir. `POST /api/runs` is unchanged
   at the API boundary — only what the subprocess does internally changed.
3. **Verified live** on real GIAB HG002 reads (Nextflow 26.04 + a JRE + the bioconda toolchain in a
   local conda env, not a repo dependency): `completed=7 failed=0`, QC parsed (Q30 88.2%, coverage
   54.2×, 553 variants), gate → HG002 HOLD (cluster_pf missing — the honest expected result, a
   run-level SAV metric a fastq→BAM path can't produce).

### 2026-07-11 (W4) — an executor-profile layer: local-serial / Slurm

T-129, commit `5f0d5ec`. Detail: [design/nextflow-codegen.md §Executor
profiles](design/nextflow-codegen.md).

1. Two baked-in `nextflow.config` profiles alongside `conda`/`docker`/`singularity`/`stub`:
   `standard` (demo default — local single-thread-serial: `queueSize=1`/`maxForks=1`/`cpus=1`) and
   `slurm` (`process.executor='slurm'`, queue / `clusterOptions` / in-flight cap all env-driven via
   `BAYLEAF_SLURM_QUEUE`/`_CLUSTER_OPTIONS`/`_QUEUE_SIZE`, never a baked guess). One sbatch job per
   process instance.
2. The driver auto-selects: `run_giab_pipeline.py`'s `_detect_profile()` picks `-profile slurm` when
   `sbatch` is on `PATH`, else `-profile standard`. The compiled bundle is identical either way.
3. Per-sample fan-out at the compiler/pipeline level: every catalogued process carries the nf-core
   `[meta, files]` map and runs once per samplesheet row (`ProcessSpec.per_sample`, default `True`;
   `MultiQC` is the one cross-sample aggregator). **Narrowed 2026-07-11 (W4 continuation, commit
   `9ab7fca`):** the driver's post-run parse (`discover_samples`/`parse_publish_dir`/
   `write_run_dir_multi`) is now N-sample capable, offline-verified against fixture publish dirs (7
   tests, `tests/test_run_giab_multisample.py`). The LIVE half remains unbuilt: the driver still
   hands Nextflow a single-row sheet (no second real sample's reads on disk in this sandbox), so a
   genuinely live multi-sample run has never been exercised.
4. **CONFIG-verified, not CLUSTER-verified.** No `sbatch` on PATH in the sandbox or the maintainer's
   local env, so every live run to date took the `standard` local-serial branch. The `slurm`
   profile's syntax has been read/reasoned but **never submitted to a real Slurm cluster**. AWS-Batch/
   HealthOmics executor config remains fully unbuilt.

---

## ADR-0018 — variant interpretation: what landed against a committed run

Canonical decision (boundary + D1/D2/D3) + current status:
[adr/ADR-0018](adr/ADR-0018-variant-interpretation-advisory-evidence.md). Design detail:
[design/variant-interpretation.md](design/variant-interpretation.md).

### 2026-07-11 — the build increments

Verified by reading `api/main.py`, `api/share_store.py`, `tests/test_route_to_human.py`,
`tests/test_share_egress.py` directly.

1. **D2 fires end-to-end against a committed run (commit `8ecc2a1`).** `api/main._active_runbook
   (run_id)` is the deployment-config seam that arms route-to-human **per run**: it reads an optional
   `route_to_human` marker file (comma-separated ClinVar significances) from the run dir and, if
   present, arms a copy of `DEFAULT_RUNBOOK`'s `RouteToHumanPolicy` for that run only; absent/empty
   stays the stock disarmed default. A new fixture `data/RUN-2026-07-11-CLINVAR-RTH/`
   (`origin=contrived`) demonstrates it: clean QC, a single verbatim-cited ClinVar **Pathogenic**
   BRCA1 spike (`VCV000017661`) HG002 does **not** actually carry, and the arming marker — HG002 now
   **ESCALATE**s via `VAR-RTH-001` when this run is evaluated through the API. Every other/unmarked
   run stays disarmed (core default + pinned demo untouched). The fixture's `NOTE.md` restates the
   honesty caveat: no real individual, no bayleaf-authored pathogenicity (ADR-0004).
2. **D3's Safe-Harbor-style scrub wired to a real egress (commits `076ecd4`, `263390a`), narrower
   than the full Share window.** `POST /api/runs/{run_id}/share` (`require_role("approver")`) runs
   every decision row through `api.safe_harbor.redact_record`, returns a `ShareBundle` (scrubbed rows
   + a `ShareManifest`: `policy_id="safe-harbor-style-v1"`, `n_rows`, `origin`, a sha256
   `content_hash` of the exact emitted bytes, `event_id`, the 18 §164.514(b)(2) classes, the
   disclaimer), and records a `DATA_EXPORTED` event to the separate share sink. Surfaced in Provenance
   as an approver-only, confirm-gated "Share (de-identified)" action. **Narrower, honestly:** no scope
   selector, no location choice, no security-level tier (the Safe-Harbor-style scrub is the only
   policy); the share audit lives only in the run's own Event trail, not (yet) the Admin Activity
   feed. 5 tests (`tests/test_share_egress.py`).
3. **Share sink brought to persistence parity (commit `9a4ef5f`).** `api/share_ledger.py` renamed +
   rebuilt as `api/share_store.py`: a `ShareStore` Protocol + `JsonlShareStore`/`SqliteShareStore`/
   `PostgresShareStore`, `BAYLEAF_SHARE_STORE` env-selected (default `jsonl`), degrade-to-JSONL on
   any construction failure (logged by exception type, never the DSN). 6 tests
   (`tests/test_share_store.py`) + a live-Postgres round-trip in
   `tests/test_persistence_postgres_live.py`, verified green against a real `postgres:16`. Parity of
   the storage backend only; endpoint behavior unchanged. **Multi-worker locking stays a documented
   seam, not built** (same limit `api/feedback_store.py` carries).
4. **A `RunReport` view (W3, commit `3d5a73d`)** — a narrower "option A" over already-wired data, not
   the full `api/report.py` projection. `RunDetail` gains a `?view=report` **Report** tab
   (`RunReport.tsx`): verdict mix, a route-to-human hero panel quoting ClinVar VERBATIM, per-sample
   gate outcomes + cited evidence, a sign-off footer stating human sign-off is a labelled seam. No
   `ReportStore`, no sign-off write path, no persisted artifact. Same commit fixed a Lineage-DAG
   honesty bug: a fired route-to-human ESCALATE used to render the review node "skipped" (no VCF
   artifact) despite the rules having escalated — a fired gate now wins over the no-artifact default.
5. **The per-variant evidence table (W3 continuation, commit `fec0f83`).** A read-only `GET
   /api/runs/{run_id}/variants` serves every `VariantCall` a run's `variants.csv` carries, parsed via
   the same `bayleaf.parsers.parse_variant_calls` the route-to-human rule uses (404 unknown run;
   `[]` when no `variants.csv`). `RunReport.tsx` renders it as a paginated table (Sample · Gene · HGVS
   · ClinVar significance quoted VERBATIM · review status · accession) with its own disclaimer.
   **Narrower than the full `AnnotatedVariant` model:** only `VariantCall`/D2 fields — no gnomAD AF, no
   inheritance-fit, no call-quality join. +3 tests (`tests/test_run_variants.py`).
6. **Genuinely still unbuilt:** the interpretation agent, the `api/report.py`/`ReportStore`/sign-off
   lifecycle, the full Share window (scope/location/security-level), gnomAD AF / inheritance-fit
   evidence, the review-ordering tier, and the ClinVar/gnomAD fetch scripts.

---

## Design docs — frontend + platform chronology (relocated 2026-07-12)

Relocated from `design/architecture.md` and `design/frontend/README.md`, which had accreted a
per-batch/per-wave changelog. Canonical current-state now lives in those docs + the ADRs; this is
the play-by-play. Each entry is grounded in the cited commit range + `docs/journal/` entry. **None
of the frontend passes changed a verdict, gate, or the ADR-0001 boundary** unless a backend item is
explicitly called out (each verified `git diff --stat … -- src/ api/ tests/` empty).

### Frontend build-out (2026-07-09 → 2026-07-10)

The React frontend was rebuilt to the refreshed design prototype (2026-07-09, T-062), then extended
across many maintainer UI-feedback passes:

1. **Batch 2** (`e891e62`→`6371128`, [journal](journal/2026-07-09-frontend-batch2.md)) — admin
   panel; wired previously-broken product writes to real endpoints; per-screen fidelity gaps.
2. **Batch 3** (`e5d5043`→`01ba673`, [journal](journal/2026-07-09-frontend-batch3.md)) — Submit
   became a real execution boundary (T-057, `api/routers/intake.py`, a **backend** item);
   decision-card three-gate readout (T-073); top-bar run switcher + F17 fix (T-074, dots read real
   lifecycle status not `n_attention`); editable germline template (T-075); signatures pagination.
3. **Batch 4** (`71a06d6`→`07f53af`, [journal](journal/2026-07-10-provenance-qc-builder-auth.md)) —
   Provenance download + full digest + real QC input (T-077); UI refinements (T-078); Grafana board
   (T-079); hash-label + chart-width (T-080); demo login gate + `isAdmin` governance (T-081); QC
   enrichment three-gate readout (T-082, a **backend** `QCMetrics` widening); Builder connector +
   tool-I/O fix + canvas nav (T-083/T-084, computed `SEEDED_WIRES`).
4. **Batch 5** (`14c9f3c`→`5774143`, T-085–T-092,
   [journal](journal/2026-07-10-batch5-builder-card-admin-prefs.md)) — flow-preserving Builder Tidy;
   Reference-source palette cards; gate-dependency `blocked_by` (DC2 part 1); decision-card pill
   polish; view-vs-download split; persisted theme/density (`PrefsContext`); Admin role-staging.
5. **Batch 6** (`8a14661`→`4208f0b`, T-093–T-096,
   [journal](journal/2026-07-10-admin-settings-builder-wiring.md)) — Admin Activity pagination;
   System observability + artifact-store card; Settings sample-type dropdown; **Dry-run/Diff wired to
   real endpoints** (T-096).
6. **Batch 7** (`34bca5d`→`adfd7aa`, T-069/T-070/T-072,
   [journal](journal/2026-07-10-builder-modals-and-run-selector.md)) — Monitoring per-run pagination;
   reusable `RunSelector` (closed T-070); Builder advisory-modal wiring + saved-profiles (closed
   T-069).
7. **Batch 8** (`5763be1`→`f8a6f35`, T-098–T-100,
   [journal](journal/2026-07-10-batch8-theme-monitoring-recharts.md)) — warm-japandi theme trial
   (later reverted); advisory-palette-in-View + digest→"fingerprint"; **Monitoring rework adds
   recharts 3.9.2 (MIT)**, a frozen scrolling `ComposedChart` (REVERSED batch 7's per-run pager).
8. **Settings agent table** (`7b579bb`, T-103,
   [journal](journal/2026-07-10-settings-agent-table.md)) — model-tiering card → paginated roster;
   still client-side only (T-045 open).
9. **Wave 7** (`52124d3`→`d832553`, T-105–T-108, [journal](journal/2026-07-10-frontend-batch7.md)) —
   theme reverted to cool-clinical + nav themeable (T-105); Builder double-grid fix + minimap
   viewport rect (T-106); Monitoring slanted dates + toggleable trend lines, review-queue neutral
   Resolve (T-107); **Inbox** — a new off-gate personal notification/triage workspace (T-108,
   REQ-F-077).
10. **Wave 8** (`1bc0072`→`109557e`, T-110–T-115, [journal](journal/2026-07-10-frontend-wave8.md)) —
    canonical `Tabs` replaces `FacetChip` (deleted) + nav reorg (T-110); Submit bulk-edit (T-111);
    intake preflight metadata grid (T-112); Inbox refinements, IB4 external-notification cadence
    DEFERRED (T-113); **Provenance rewrite** into Lineage/Event-trail/Artifacts over real
    `RunDetail.events`, zero backend change (T-114); Pipeline-Builder on-canvas editing (T-115).
11. **Wave 9** (`3e592d8`→`66b14e4`, T-116–T-117, [journal](journal/2026-07-10-frontend-wave9.md)) —
    canonical `Bar` geometry + first `Truncate` use (T-116); **page-access RBAC view-gate**
    (`access.ts`, gates VIEWS not API) + a **sample-accessioning CRM screen** (`Accession.tsx`), all
    client-side, no PII transmitted (T-117). Operator screen count reached 12.
12. **Confirm-dialog audit retrofit** (`d65c9c1`,
    [journal](journal/2026-07-10-confirm-dialog-audit-gate.md)) — `ConfirmDialog`/`useConfirm()`
    gates stakes-y off-gate writes (ADR-0017 UX realization).

### Backend + platform waves (2026-07-10 → 2026-07-11)

1. **Wave 10** (`71d4ff9`→`6b571a4`, T-046/T-118,
   [journal](journal/2026-07-10-wave10-node-author-uic.md)) — **node-authoring agent (backend)**, the
   sixth stub|claude AI seam; and **UIC-1..16** (see [design/ui-conventions.md](design/ui-conventions.md)).
2. **Wave 11** (`8ecc2a1`,`12a9913`, [journal](journal/2026-07-11-d2-d3-share-egress.md)) — D2/D3
   (see the ADR-0018 section above); UIC-16 closed (`NODE_W=232` four-sided ports).
3. **Wave 12** (`a03704f`→`3d531de`, T-124,
   [journal](journal/2026-07-11-builder-boundary-and-edges.md)) — Builder edge clarity + the
   deterministic gate/ingest **removed from the canvas** into a read-only `DecisionBoundaryModal` (a
   UI-level reinforcement of ADR-0001 Realized §3).
4. **P3 backlog waves 1–2** (`595815e`,`deee99f`, T-131/T-132,
   [journal](journal/2026-07-11-p3-backlog.md)) — durable job store (`api/job_store.py`); intake
   pre-flight guards + version capture; registry honesty labeling; agent-safety hardening; UI a11y +
   contract-type drift; `GET /api/monitoring` server-side `page`/`limit` (closed T-072).
5. **Audit + W1–W4 + E2E** (`c71fb6c`→`2e9b4e5`, T-125–T-130,
   [journal](journal/2026-07-11-audit-hardening-w1-w4-e2e.md)) — Fable release-hardening audit
   (`audit/`); W1 approval gate on `POST /api/pipelines/run`; W2 node-author read path; W3 Report tab
   + honest downstream provenance; W4 executor profiles + per-sample fan-out + full port wiring;
   offline E2E acceptance test.
6. **W3/W4 continuations** (`fec0f83`,`9ab7fca`, T-133/T-134,
   [journal](journal/2026-07-11-w-deferrals.md)) — `GET /api/runs/{id}/variants` + per-variant report
   table; N-sample driver parse (offline-verified vs fixture publish dirs).
7. **Fleet** (`5a3dd6a`,`4427ec2`,`63b87ce`, T-135–T-137, [journal](journal/2026-07-11-fleet.md)) —
   node-author **accept→library** backend (`api/library_store.py`, `conformance.py`, `importer.py`);
   a11y baseline extended; reliability tests (`killpg` reap, atomic dup-run-id, builder-run
   lost-recovery).
8. **Custom-script I/O + sandboxed file browser** (`feat/custom-script-io`, `6f1c758`,
   [journal](journal/2026-07-11-custom-script-io.md)) — ADR-0020; retired `Truth VCF` + `NGSCheckMate`
   palette nodes for a generic **File input** card; operator-authored custom-script processes
   (`NfNode.script/container/conda`); sandboxed `GET /api/files` (`api/routers/files.py`); dropped
   `source_truth_vcf` from the node-author corpus (11 → 10).

### Builder agent hardening (2026-07-12, T-142/T-143)

Canonical current-state: [requirements/functional.md](requirements/functional.md) REQ-F-101/102/103,
[requirements/nonfunctional.md](requirements/nonfunctional.md) REQ-NF-028/054,
[quality/evaluation.md](quality/evaluation.md) EVAL-019/EVAL-052. Detail:
[journal 2026-07-12](journal/2026-07-12-builder-agent-hardening.md). Arc base `14307f7`, 5 commits
`e40784c`→`7c5c073`.

1. **Agent-observation binding model** (`69a2dab`, frontend + `types.ts` only). The ephemeral
   `advisoryAttach: Set` became a typed, persisted `AgentBinding {agent, node, grants:('outputs'|
   'logs')[]}` in a `graph.agent_bindings` envelope key **the compiler never dereferences** — proven
   byte-identical compile with/without bindings by construction (compile payload is `{nodes, edges}`
   only; `CompileRequest` is pydantic `extra="ignore"`). Taxonomy: Pipeline-repair + Archivist moved
   out of the Builder palette to Agent-triage launchers; the Builder keeps QC-triage (node-attachable)
   + Node-authoring.
2. **Phase 4 — the scoped, de-identified agent-read** (`4582c7d`, backend + 8 tests).
   `GET /api/runs/{id}/nodes/{node}/observations?grants=outputs[,logs]` returns a bound node's
   published outputs (scoped by the tool's catalogued output-port globs) and an opt-in
   **de-identified** log tail (`api/deid.py` `scrub_text()` — subject ids pseudonymized, email/6+-digit
   PII redacted; a test plants subject-id/email/MRN and proves all three scrubbed). Read-only,
   traversal-hardened, honest-empty. `gather_node_observations()` is the triage-consumption seam;
   agent consumption + a UI display + the authored-node→graph linkage stay deferred.
3. **Reserved-port honesty** (`1621e3f`, +5 tests). Every shown Builder port now maps to a real
   emitted Nextflow channel or was removed (promoted fastp `unpaired_fastq`/`failed_fastq`,
   bcftools-norm `vcf_index`, MultiQC `multiqc_html`; removed `read_group`/`per_base`/`panel_bed`/
   `fastqc_zip`/`bcftools_stats`/`picard_hsmetrics`/`ngscheckmate`; fastp `adapter_fasta` left
   honestly-reserved).
4. **mosdepth Export-to-Nextflow 422 fix + Builder UX + `tsc -b` pre-push gate** (`e40784c`,
   `7c5c073`). The mosdepth card advertised 5 outputs vs a 2-output catalog → the arity gap tripped
   the output-drift guard and 422'd Export-to-Nextflow of the default view; the 3 real byproducts are
   now catalogued (germline drift green). Builder UX polish (phantom port removed, honestly
   non-armable reserved ports, legend + Provenance scroll indicator); inspector `Save card`→`Save`
   grouped with Delete in one footer row. A `frontend-tsc` pre-push hook now runs `tsc -b` (the
   references-only root tsconfig had made `tsc --noEmit` a no-op, letting a type error reach `main`).

**Not part of this arc (grounding correction):** the Nextflow compiler's Groovy/identifier injection
escaping and the File-input `_source_channel` routing the arc brief mentioned landed earlier in
`37e54a8` (T-140, an ancestor of the arc base `14307f7`), already documented as REQ-NF-046 /
EVAL-017 — no new NF REQ was written for them.

### Corrections folded into current docs (2026-07-12)

1. **Node-author corpus 11 → 10 → 9.** Truth VCF retired (`feat/custom-script-io` Branch A, 11→10);
   then **NGSCheckMate retired-but-pinned** from the *proposable* corpus (10→9): its card is
   commented out in `tool_cards.jsonl` so `load_tool_card_corpus()` skips it, but the `ngscheckmate`
   KIND stays in the vocabulary so a File-input/custom-script card can still emit it. Current
   canonical count: **9** (7 germline tools + Reference FASTA + Panel BED).
2. **ADR-0021** (operator-gated/scheduled sample processing: intake can run an approved authored
   pipeline by name, + `mode` immediate/hold/schedule + `POST /api/runs/{id}/release`) folded into
   the current design docs alongside **ADR-0020** (operator-authored custom-script processes).
3. **Compiler robustness hardening** (`tests/test_nextflow_robustness.py`, 17 cases) — Groovy/
   identifier injection escaping, the File-input-source-wires-to-reads fix, proc-name-collision /
   fan-in / duplicate-emit / catalog-port-drift guards — folded into
   [design/nextflow-codegen.md](design/nextflow-codegen.md) §Wiring rules.

### Test-census milestones (relocated from quality/evaluation.md, 2026-07-12)

The census in [quality/evaluation.md](quality/evaluation.md) now states only the HEAD figure
(re-derived each sweep via `uv run pytest --collect-only -q` + `git ls-files 'tests/*.py' | wc -l`).
The prior collected/file milestones, kept here for reconstructing *when* coverage grew:

1. **634 / 48** — 2026-07-12 (Builder agent hardening, T-142/T-143; `test_node_observations` 8 +
   `test_nextflow_promoted_ports` 5 + a mosdepth-5-output regression in `test_nextflow_compile`
   15→16 added). Offline (no `nextflow`, no live Postgres): **627 pass / 7 skip**.
2. **620 / 46** — 2026-07-12 (bayleaf rebrand + NGSCheckMate retire + compiler robustness T-140 +
   ADR-0021 intake scheduling T-141; `test_nextflow_robustness` 17 + `test_intake_scheduling` 15
   added). Offline (no `nextflow`, no live Postgres): **613 pass / 7 skip**.
3. **585 / 44** — 2026-07-11, custom-script-I/O session (`feat/custom-script-io`; 578 pass / 7 skip
   offline).
4. **555 / 41** — after the "fleet" session (T-135–T-137).
5. **517 / 37** — after the W3/W4-deferred-slice continuation (T-133/T-134).
6. **507 / 35** — after the P3-backlog session (T-131/T-132).
7. **471 / 33** — after the audit / hardening / W1–W4 / E2E session (T-125–T-130).
8. **427 / 29** — before that (Nextflow-codegen-executable session, T-123).

The pass/skip split has always depended on whether `nextflow` is on `PATH` (the machine-gated
live-integration checks) — see the current doc for the HEAD breakdown of the 7 skips (3
`nextflow`-gated + 4 Postgres-live).
